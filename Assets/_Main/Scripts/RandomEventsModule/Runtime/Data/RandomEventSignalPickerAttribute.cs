using UnityEngine;

namespace RandomEventsModule
{
    /// <summary>Draws a <c>string</c> field as a dropdown over every <see cref="RandomEventSignalKeyAttribute"/> constant, instead of free text.</summary>
    public sealed class RandomEventSignalPickerAttribute : PropertyAttribute
    {
    }
}
