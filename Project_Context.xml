This file is a merged representation of a subset of the codebase, containing specifically included files and files not matching ignore patterns, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of a subset of the repository's contents that is considered the most important context.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
- Pay special attention to the Repository Description. These contain important context and guidelines specific to this project.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Only files matching these patterns are included: Assets/_Main/Scripts/**/*.cs, Assets/_Main/Scripts/**/*.asmdef
- Files matching these patterns are excluded: **/*.meta, **/*.mat, **/*.prefab, **/*.unity, **/*.asset, **/*.png, **/*.jpg, **/*.ogg, **/*.wav, **/*.mp3, **/*.pdf, **/InputSystem_Actions.cs, **/Library/**, **/Temp/**, **/Obj/**, **/Build/**, **/Packages/**, **/ProjectSettings/**, **/UserSettings/**, **/.vs/**, **/.git/**
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<user_provided_header>
Unity Project Context...
</user_provided_header>

<directory_structure>
Assets/_Main/Scripts/BuildingModule/BuildingModule.asmdef
Assets/_Main/Scripts/BuildingModule/Debug/GridOverlay.cs
Assets/_Main/Scripts/BuildingModule/Runtime/Commands/SelectBuildingCommand.cs
Assets/_Main/Scripts/BuildingModule/Runtime/Controllers/ModeControllers.cs
Assets/_Main/Scripts/BuildingModule/Runtime/Controllers/PlacementServiceController.cs
Assets/_Main/Scripts/BuildingModule/Runtime/Core/BuildingFactory.cs
Assets/_Main/Scripts/BuildingModule/Runtime/Core/CampGrid.cs
Assets/_Main/Scripts/BuildingModule/Runtime/Core/IGrid.cs
Assets/_Main/Scripts/BuildingModule/Runtime/Core/Tile.cs
Assets/_Main/Scripts/BuildingModule/Runtime/Data/BuildingCatalog.cs
Assets/_Main/Scripts/BuildingModule/Runtime/Data/BuildingConfig.cs
Assets/_Main/Scripts/BuildingModule/Runtime/Data/BuildingName.cs
Assets/_Main/Scripts/BuildingModule/Runtime/Data/GridConfig.cs
Assets/_Main/Scripts/BuildingModule/Runtime/Installers/BuildingModuleInstaller.cs
Assets/_Main/Scripts/BuildingModule/Runtime/Presenters/ConstructionModePresenter.cs
Assets/_Main/Scripts/BuildingModule/Runtime/Services/ConstructionModeService.cs
Assets/_Main/Scripts/BuildingModule/Runtime/Services/IConstructionModeService.cs
Assets/_Main/Scripts/BuildingModule/Runtime/Services/IPlacementService.cs
Assets/_Main/Scripts/BuildingModule/Runtime/Services/IPreviewService.cs
Assets/_Main/Scripts/BuildingModule/Runtime/Services/PlacementService.cs
Assets/_Main/Scripts/BuildingModule/Runtime/Services/PreviewService.cs
Assets/_Main/Scripts/BuildingModule/Runtime/View/BuildingView.cs
Assets/_Main/Scripts/BuildingModule/Runtime/View/Buttons/SelectBuildingButton.cs
Assets/_Main/Scripts/BuildingModule/Runtime/View/GridView.cs
Assets/_Main/Scripts/CameraModule/CameraModule.asmdef
Assets/_Main/Scripts/CameraModule/Runtime/BuildingMode/Controllers/BuildingModeCameraController.cs
Assets/_Main/Scripts/CameraModule/Runtime/BuildingMode/Core/BuildingModeCameraService.cs
Assets/_Main/Scripts/CameraModule/Runtime/BuildingMode/Core/IBuildingModeCameraService.cs
Assets/_Main/Scripts/CameraModule/Runtime/Installers/CameraInstaller.cs
Assets/_Main/Scripts/CommandsModule/CommandsModule.asmdef
Assets/_Main/Scripts/CommandsModule/Runtime/Core/IAsyncCommand.cs
Assets/_Main/Scripts/CommandsModule/Runtime/Core/TaskResult.cs
Assets/_Main/Scripts/CommandsModule/Runtime/View/ExecuteCommandButton.cs
Assets/_Main/Scripts/ComponentsModule/ComponentsModule.asmdef
Assets/_Main/Scripts/ComponentsModule/Runtime/Crouch/CrouchComponent.cs
Assets/_Main/Scripts/ComponentsModule/Runtime/Crouch/CrouchData.cs
Assets/_Main/Scripts/ComponentsModule/Runtime/Crouch/ICrouchComponent.cs
Assets/_Main/Scripts/ComponentsModule/Runtime/Health/DamageHandler.cs
Assets/_Main/Scripts/ComponentsModule/Runtime/Health/HealthComponent.cs
Assets/_Main/Scripts/ComponentsModule/Runtime/Health/IDamageable.cs
Assets/_Main/Scripts/ComponentsModule/Runtime/Health/IHealthComponent.cs
Assets/_Main/Scripts/ComponentsModule/Runtime/Lean/ILeanComponent.cs
Assets/_Main/Scripts/ComponentsModule/Runtime/Lean/LeanComponent.cs
Assets/_Main/Scripts/ComponentsModule/Runtime/Movement/IMoveComponent.cs
Assets/_Main/Scripts/ComponentsModule/Runtime/Movement/MoveComponent.cs
Assets/_Main/Scripts/ComponentsModule/Runtime/Rotation/IRotationComponent.cs
Assets/_Main/Scripts/ComponentsModule/Runtime/Rotation/RotationComponent.cs
Assets/_Main/Scripts/EnemyModule/Configs/EnemyConfig.cs
Assets/_Main/Scripts/EnemyModule/Controllers/EnemyController.cs
Assets/_Main/Scripts/EnemyModule/Core/EnemyModel.cs
Assets/_Main/Scripts/EnemyModule/Data/AIState.cs
Assets/_Main/Scripts/EnemyModule/EnemyModule.asmdef
Assets/_Main/Scripts/EnemyModule/Installers/EnemyInstaller.cs
Assets/_Main/Scripts/EnemyModule/View/EnemyRagdoll.cs
Assets/_Main/Scripts/EnemyModule/View/EnemyView.cs
Assets/_Main/Scripts/ExtractionModule/Configs/ExtractionConfig.cs
Assets/_Main/Scripts/ExtractionModule/Controllers/ExtractionController.cs
Assets/_Main/Scripts/ExtractionModule/ExtractionModule.asmdef
Assets/_Main/Scripts/ExtractionModule/Installers/ExtractionInstaller.cs
Assets/_Main/Scripts/ExtractionModule/View/ExtractionHUDView.cs
Assets/_Main/Scripts/ExtractionModule/View/ExtractionZoneView.cs
Assets/_Main/Scripts/GameCycleModule/GameCycleModule.asmdef
Assets/_Main/Scripts/GameCycleModule/Runtime/Core/GameSessionService.cs
Assets/_Main/Scripts/GameCycleModule/Runtime/Core/IGameSessionService.cs
Assets/_Main/Scripts/GameCycleModule/Runtime/Installers/GameCycleInstaller.cs
Assets/_Main/Scripts/GameCycleModule/Runtime/Presenters/GameOverPresenter.cs
Assets/_Main/Scripts/GameCycleModule/Runtime/View/GameOverView.cs
Assets/_Main/Scripts/InputModule/Core/GameInputService.cs
Assets/_Main/Scripts/InputModule/Core/IInputMap.cs
Assets/_Main/Scripts/InputModule/Core/IInputService.cs
Assets/_Main/Scripts/InputModule/InputModule.asmdef
Assets/_Main/Scripts/InputModule/Installers/InputInstaller.cs
Assets/_Main/Scripts/InventoryModule/InventoryModule.asmdef
Assets/_Main/Scripts/InventoryModule/Runtime/Core/GridCell.cs
Assets/_Main/Scripts/InventoryModule/Runtime/Core/InventoryItem.cs
Assets/_Main/Scripts/InventoryModule/Runtime/Core/InventoryModel.cs
Assets/_Main/Scripts/InventoryModule/Runtime/Data/InventoryConfig.cs
Assets/_Main/Scripts/InventoryModule/Runtime/Data/ItemDatabase.cs
Assets/_Main/Scripts/InventoryModule/Runtime/Data/ItemDefinition.cs
Assets/_Main/Scripts/InventoryModule/Runtime/Installers/InventoryGlobalInstaller.cs
Assets/_Main/Scripts/InventoryModule/Runtime/Installers/InventorySceneInstaller.cs
Assets/_Main/Scripts/InventoryModule/Runtime/Presenter/InventoryPresenter.cs
Assets/_Main/Scripts/InventoryModule/Runtime/Services/GridService.cs
Assets/_Main/Scripts/InventoryModule/Runtime/Services/IInventoryService.cs
Assets/_Main/Scripts/InventoryModule/Runtime/Services/IItemProvider.cs
Assets/_Main/Scripts/InventoryModule/Runtime/Services/InventoryOperationResult.cs
Assets/_Main/Scripts/InventoryModule/Runtime/Services/InventoryService.cs
Assets/_Main/Scripts/InventoryModule/Runtime/Services/ItemProvider.cs
Assets/_Main/Scripts/InventoryModule/Runtime/View/InventoryItemView.cs
Assets/_Main/Scripts/InventoryModule/Runtime/View/InventoryView.cs
Assets/_Main/Scripts/InventoryModule/Runtime/View/LootItemView.cs
Assets/_Main/Scripts/NpcBaseModule/NpcBaseModule.asmdef
Assets/_Main/Scripts/NpcBaseModule/Runtime/Controllers/FriendlyNpcController.cs
Assets/_Main/Scripts/NpcBaseModule/Runtime/Core/IInteractable.cs
Assets/_Main/Scripts/NpcBaseModule/Runtime/Core/PlayerInteractor.cs
Assets/_Main/Scripts/NpcBaseModule/Runtime/Data/FriendlyNpcSettings.cs
Assets/_Main/Scripts/NpcBaseModule/Runtime/Data/FriendlyNpcState.cs
Assets/_Main/Scripts/NpcBaseModule/Runtime/Installers/FriendlyNpcInstaller.cs
Assets/_Main/Scripts/NpcBaseModule/Runtime/View/FriendlyNpcView.cs
Assets/_Main/Scripts/NpcBaseModule/Runtime/View/NpcWorldTextView.cs
Assets/_Main/Scripts/PlayerModule/PlayerModule.asmdef
Assets/_Main/Scripts/PlayerModule/Runtime/Controllers/InventoryDeathHandler.cs
Assets/_Main/Scripts/PlayerModule/Runtime/Controllers/PlayerInteractionController.cs
Assets/_Main/Scripts/PlayerModule/Runtime/Controllers/PlayerMovementController.cs
Assets/_Main/Scripts/PlayerModule/Runtime/Core/PlayerProvider.cs
Assets/_Main/Scripts/PlayerModule/Runtime/Data/PlayerConfig.cs
Assets/_Main/Scripts/PlayerModule/Runtime/Installers/PlayerInstaller.cs
Assets/_Main/Scripts/UIModule/Runtime/Presenters/HealthPresenter.cs
Assets/_Main/Scripts/UIModule/Runtime/View/HealthView.cs
Assets/_Main/Scripts/UIModule/UIModule.asmdef
Assets/_Main/Scripts/WeaponModule/Content/Bullet.cs
Assets/_Main/Scripts/WeaponModule/Controllers/WeaponController.cs
Assets/_Main/Scripts/WeaponModule/Controllers/WeaponInventory.cs
Assets/_Main/Scripts/WeaponModule/Core/IWeapon.cs
Assets/_Main/Scripts/WeaponModule/Core/WeaponFactory.cs
Assets/_Main/Scripts/WeaponModule/Core/WeaponModel.cs
Assets/_Main/Scripts/WeaponModule/Data/CameraRecoilSettings.cs
Assets/_Main/Scripts/WeaponModule/Data/RecoilSettings.cs
Assets/_Main/Scripts/WeaponModule/Data/SwaySettings.cs
Assets/_Main/Scripts/WeaponModule/Data/WeaponConfig.cs
Assets/_Main/Scripts/WeaponModule/Data/WeaponSetupData.cs
Assets/_Main/Scripts/WeaponModule/Installers/WeaponInstaller.cs
Assets/_Main/Scripts/WeaponModule/View/Procedural/RecoilProcessor.cs
Assets/_Main/Scripts/WeaponModule/View/Procedural/SwayProcessor.cs
Assets/_Main/Scripts/WeaponModule/View/WeaponView.cs
Assets/_Main/Scripts/WeaponModule/WeaponModule.asmdef
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="Assets/_Main/Scripts/InventoryModule/Runtime/Data/ItemDatabase.cs">
using System.Collections.Generic;
using System.Linq;
using UnityEngine;

namespace InventoryModule
{
    [CreateAssetMenu(fileName = "ItemDatabase", menuName = "Game/Inventory/Item Database")]
    public class ItemDatabase : ScriptableObject
    {
        [SerializeField] private List<ItemDefinition> items = new();

        private Dictionary<string, ItemDefinition> _cache;

        public IReadOnlyList<ItemDefinition> AllItems => items;

        public ItemDefinition GetItem(string itemId)
        {
            if (_cache == null)
                BuildCache();

            if (_cache.TryGetValue(itemId, out var item))
                return item;

            return null;
        }

        public bool TryGetItem(string itemId, out ItemDefinition item)
        {
            item = GetItem(itemId);
            return item != null;
        }

        public List<ItemDefinition> GetItemsByCategory(ItemCategory category)
        {
            return items.Where(item => item.Category == category).ToList();
        }

        public bool ContainsItem(string itemId)
        {
            return items.Any(item => item.Id == itemId);
        }

        private void BuildCache()
        {
            _cache = new Dictionary<string, ItemDefinition>();

            foreach (var item in items)
            {
                if (item == null) continue;

                if (!_cache.ContainsKey(item.Id))
                {
                    _cache[item.Id] = item;
                }
            }
        }

#if UNITY_EDITOR
        private void OnValidate()
        {
            _cache = null;

            var seenIds = new HashSet<string>();
            foreach (var item in items)
            {
                if (item == null) continue;

                if (seenIds.Contains(item.Id))
                {
                }
                else
                {
                    seenIds.Add(item.Id);
                }
            }
        }

        [ContextMenu("Sort Alphabetically")]
        private void SortAlphabetically()
        {
            items = items.OrderBy(x => x.ItemName).ToList();
            UnityEditor.EditorUtility.SetDirty(this);
        }
#endif
    }
}
</file>

<file path="Assets/_Main/Scripts/InventoryModule/Runtime/Data/ItemDefinition.cs">
using UnityEngine;

namespace InventoryModule
{
    [CreateAssetMenu(fileName = "NewItem", menuName = "Game/Inventory/Item")]
    public class ItemDefinition : ScriptableObject
    {
        [SerializeField] private string itemName;
        [SerializeField, TextArea(3, 5)] private string description;
        [SerializeField] private Sprite icon;
        [SerializeField] private GameObject worldPrefab;
        [SerializeField] private GameObject inventoryPrefab;

        [Min(1)][SerializeField] private int width = 1;
        [Min(1)][SerializeField] private int height = 1;
        [Min(1)][SerializeField] private int maxStack = 1;
        [SerializeField] private bool canRotate = true;

        [SerializeField] private float weight = 0.1f;
        [SerializeField] private int value = 1;
        [SerializeField] private ItemCategory category = ItemCategory.Misc;

        private string _cachedId;

        public string Id
        {
            get
            {
                if (string.IsNullOrEmpty(_cachedId))
                {
                    _cachedId = GenerateId(itemName);
                }
                return _cachedId;
            }
        }

        public string ItemName => itemName;
        public string Description => description;
        public Sprite Icon => icon;
        public GameObject WorldPrefab => worldPrefab;
        public GameObject InventoryPrefab => inventoryPrefab;
        public int Width => width;
        public int Height => height;
        public int MaxStack => maxStack;
        public bool CanRotate => canRotate;
        public float Weight => weight;
        public int Value => value;
        public ItemCategory Category => category;

        private string GenerateId(string baseName)
        {
            if (string.IsNullOrEmpty(baseName))
                return "item_unknown";

            var id = baseName.ToLower()
                .Replace(" ", "_")
                .Replace("-", "_")
                .Replace("'", "")
                .Replace("\"", "");

            var chars = id.ToCharArray();
            for (int i = 0; i < chars.Length; i++)
            {
                if (!char.IsLetterOrDigit(chars[i]) && chars[i] != '_')
                {
                    chars[i] = '_';
                }
            }

            return new string(chars);
        }

#if UNITY_EDITOR
        private void OnValidate()
        {
            _cachedId = null;

            if (!string.IsNullOrEmpty(itemName))
            {
                _cachedId = GenerateId(itemName);
            }
        }
#endif
    }

    public enum ItemCategory
    {
        Misc = 0,
        Resource,
        Weapon,
        Ammo,
        Medical,
        Food,
        Tool,
        Quest,
        Equipment
    }
}
</file>

<file path="Assets/_Main/Scripts/InventoryModule/Runtime/Installers/InventoryGlobalInstaller.cs">
using UnityEngine;
using Zenject;

namespace InventoryModule
{
    public sealed class InventoryGlobalInstaller : MonoInstaller
    {
        [SerializeField] private InventoryConfig inventoryConfig;
        [SerializeField] private ItemDatabase itemDatabase;

        public override void InstallBindings()
        {
            Container.Bind<InventoryConfig>()
                .FromInstance(inventoryConfig)
                .AsSingle();

            Container.Bind<ItemDatabase>()
                .FromInstance(itemDatabase)
                .AsSingle();

            Container.Bind<InventoryModel>()
                .AsSingle()
                .WithArguments(inventoryConfig.Width, inventoryConfig.Height);

            Container.Bind<IItemProvider>()
                .To<ItemProvider>()
                .AsSingle();

            Container.Bind<IInventoryService>()
                .To<InventoryService>()
                .AsSingle();
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/InventoryModule/Runtime/Installers/InventorySceneInstaller.cs">
using UnityEngine;
using Zenject;

namespace InventoryModule
{
    public sealed class InventorySceneInstaller : MonoInstaller
    {
        [SerializeField] private InventoryView inventoryView;

        public override void InstallBindings()
        {
            Container.Bind<InventoryView>()
                .FromInstance(inventoryView)
                .AsSingle();

            Container.BindInterfacesTo<InventoryPresenter>()
                .AsSingle()
                .NonLazy();
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/InventoryModule/Runtime/Services/GridService.cs">
using System.Collections.Generic;
using UnityEngine;

namespace InventoryModule
{
    public sealed class GridService
    {
        private readonly GridCell[,] _cells;
        private readonly int _width;
        private readonly int _height;

        public int Width => _width;
        public int Height => _height;

        public GridService(int width, int height)
        {
            _width = width;
            _height = height;
            _cells = new GridCell[width, height];

            for (int x = 0; x < width; x++)
            {
                for (int y = 0; y < height; y++)
                {
                    _cells[x, y] = new GridCell(new Vector2Int(x, y));
                }
            }
        }

        public bool IsCellOccupied(int x, int y)
        {
            if (x < 0 || x >= _width || y < 0 || y >= _height)
                return true;

            return _cells[x, y].IsOccupied;
        }

        public GridCell GetCell(int x, int y)
        {
            if (x < 0 || x >= _width || y < 0 || y >= _height)
                return null;

            return _cells[x, y];
        }

        public bool IsAreaFree(Vector2Int position, Vector2Int size, InventoryItem ignoreItem = null)
        {
            if (position.x < 0 || position.y < 0 ||
                position.x + size.x > _width || position.y + size.y > _height)
                return false;

            for (int x = position.x; x < position.x + size.x; x++)
            {
                for (int y = position.y; y < position.y + size.y; y++)
                {
                    var cell = GetCell(x, y);
                    if (cell != null && cell.IsOccupied && cell.OccupyingItem != ignoreItem)
                        return false;
                }
            }

            return true;
        }

        public bool CanPlaceItem(InventoryItem item, Vector2Int position, bool isRotated)
        {
            if (item == null)
                return false;

            Vector2Int size = item.GetSize(isRotated);
            return IsAreaFree(position, size, item);
        }

        public void PlaceItem(InventoryItem item, Vector2Int position, bool isRotated)
        {
            Vector2Int size = item.GetSize(isRotated);
            if (!IsAreaFree(position, size, item))
            {
                Debug.LogWarning($"Cannot place item at position {position} with size {size}");
                return;
            }

            ClearItem(item);

            for (int x = position.x; x < position.x + size.x; x++)
            {
                for (int y = position.y; y < position.y + size.y; y++)
                {
                    _cells[x, y].Occupy(item);
                }
            }

            item.Position = position;
            item.IsRotated = isRotated;
        }

        public void ClearItem(InventoryItem item)
        {
            for (int x = 0; x < _width; x++)
            {
                for (int y = 0; y < _height; y++)
                {
                    if (_cells[x, y].OccupyingItem == item)
                    {
                        _cells[x, y].Vacate();
                    }
                }
            }
        }

        public bool TrySwapItems(InventoryItem item1, Vector2Int pos1, bool rot1,
                                InventoryItem item2, Vector2Int pos2, bool rot2)
        {
            Vector2Int originalPos1 = item1.Position;
            Vector2Int originalPos2 = item2.Position;
            bool originalRot1 = item1.IsRotated;
            bool originalRot2 = item2.IsRotated;

            ClearItem(item1);
            ClearItem(item2);

            Vector2Int size1 = item1.GetSize(rot1);
            Vector2Int size2 = item2.GetSize(rot2);
            if (!IsAreaFree(pos1, size1) || !IsAreaFree(pos2, size2))
            {
                PlaceItem(item1, originalPos1, originalRot1);
                PlaceItem(item2, originalPos2, originalRot2);
                return false;
            }

            PlaceItem(item1, pos1, rot1);
            PlaceItem(item2, pos2, rot2);
            return true;
        }

        public bool AreasOverlap(Vector2Int pos1, Vector2Int size1, Vector2Int pos2, Vector2Int size2)
        {
            bool overlapX = pos1.x < pos2.x + size2.x && pos1.x + size1.x > pos2.x;
            bool overlapY = pos1.y < pos2.y + size2.y && pos1.y + size1.y > pos2.y;
            return overlapX && overlapY;
        }

        public List<InventoryItem> GetItemsAtArea(Vector2Int position, Vector2Int size)
        {
            var items = new List<InventoryItem>();

            for (int x = position.x; x < position.x + size.x; x++)
            {
                for (int y = position.y; y < position.y + size.y; y++)
                {
                    var cell = GetCell(x, y);
                    if (cell != null && cell.IsOccupied)
                    {
                        if (!items.Contains(cell.OccupyingItem))
                        {
                            items.Add(cell.OccupyingItem);
                        }
                    }
                }
            }

            return items;
        }

        public InventoryItem GetItemAtPosition(Vector2Int position)
        {
            foreach (var cell in _cells)
            {
                if (cell.IsOccupied && cell.OccupyingItem.Position == position)
                {
                    return cell.OccupyingItem;
                }
            }

            return null;
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/InventoryModule/Runtime/Services/IInventoryService.cs">
using System;
using System.Threading;
using Cysharp.Threading.Tasks;
using UnityEngine;
using System.Collections.Generic;

namespace InventoryModule
{
    public interface IInventoryService
    {
        event Action InventoryUpdated;

        UniTask<InventoryOperationResult> AddItemAsync(
            ItemDefinition itemDefinition,
            int amount,
            CancellationToken token);

        UniTask<InventoryOperationResult> MoveItemAsync(
            InventoryItem item,
            Vector2Int newPosition,
            bool isRotated,
            CancellationToken token);

        UniTask<InventoryOperationResult> SplitItemAsync(
            InventoryItem item,
            Vector2Int splitPosition,
            bool isRotated,
            CancellationToken token);

        UniTask<InventoryOperationResult> RemoveItemAsync(
            InventoryItem item,
            CancellationToken token);

        bool CanPlaceItem(ItemDefinition itemDefinition, Vector2Int position, bool isRotated);
        InventoryItem GetItemAtPosition(Vector2Int position);
        IReadOnlyList<InventoryItem> GetAllItems();
    }
}
</file>

<file path="Assets/_Main/Scripts/InventoryModule/Runtime/Services/IItemProvider.cs">
using UnityEngine;

namespace InventoryModule
{
    public interface IItemProvider
    {
        ItemDefinition GetItemDefinition(string itemId);
        bool TryGetItemDefinition(string itemId, out ItemDefinition itemDefinition);
        Sprite GetItemIcon(string itemId);
    }
}
</file>

<file path="Assets/_Main/Scripts/InventoryModule/Runtime/Services/InventoryOperationResult.cs">
namespace InventoryModule
{
    public class InventoryOperationResult
    {
        public bool Success { get; }
        public string ErrorMessage { get; }
        public InventoryItem AffectedItem { get; }

        private InventoryOperationResult(bool success, string errorMessage, InventoryItem affectedItem)
        {
            Success = success;
            ErrorMessage = errorMessage;
            AffectedItem = affectedItem;
        }

        public static InventoryOperationResult CreateSuccess(InventoryItem item = null)
            => new InventoryOperationResult(true, string.Empty, item);

        public static InventoryOperationResult CreateFailure(string errorMessage, InventoryItem item = null)
            => new InventoryOperationResult(false, errorMessage, item);
    }
}
</file>

<file path="Assets/_Main/Scripts/InventoryModule/Runtime/Services/InventoryService.cs">
using System;
using System.Collections.Generic;
using System.Threading;
using Cysharp.Threading.Tasks;
using UnityEngine;

namespace InventoryModule
{
    public sealed class InventoryService : IInventoryService
    {
        private readonly InventoryModel _model;
        private readonly GridService _gridService;
        private readonly ItemDatabase _itemDatabase;

        public event Action InventoryUpdated;

        public InventoryService(InventoryModel model, ItemDatabase itemDatabase)
        {
            _model = model ?? throw new ArgumentNullException(nameof(model));
            _itemDatabase = itemDatabase ?? throw new ArgumentNullException(nameof(itemDatabase));
            _gridService = new GridService(model.Width, model.Height);
            InitializeGridWithExistingItems();
        }

        private void InitializeGridWithExistingItems()
        {
            foreach (var item in _model.Items)
            {
                _gridService.PlaceItem(item, item.Position, item.IsRotated);
            }
        }
        public UniTask<InventoryOperationResult> AddItemAsync(
            ItemDefinition itemDefinition,
            int amount,
            CancellationToken token)
        {
            if (itemDefinition == null)
                return UniTask.FromResult(InventoryOperationResult.CreateFailure("Item definition is null"));

            if (amount <= 0)
                return UniTask.FromResult(InventoryOperationResult.CreateFailure("Amount must be positive"));

            token.ThrowIfCancellationRequested();

            try
            {
                int remainingAmount = amount;

                if (itemDefinition.MaxStack > 1)
                {
                    remainingAmount = TryStackExistingItems(itemDefinition, remainingAmount);
                    if (remainingAmount <= 0)
                    {
                        InventoryUpdated?.Invoke();
                        return UniTask.FromResult(InventoryOperationResult.CreateSuccess());
                    }
                }

                while (remainingAmount > 0)
                {
                    var placementResult = TryFindPlaceForItem(itemDefinition, out var position, out var isRotated);
                    if (!placementResult.Success)
                    {
                        return UniTask.FromResult(InventoryOperationResult.CreateFailure(
                            $"No space for item: {itemDefinition.ItemName}"));
                    }

                    int stackAmount = Mathf.Min(remainingAmount, itemDefinition.MaxStack);
                    var newItem = CreateInventoryItem(itemDefinition, position, stackAmount, isRotated);

                    _gridService.PlaceItem(newItem, position, isRotated);
                    _model.AddItem(newItem);

                    remainingAmount -= stackAmount;
                }

                InventoryUpdated?.Invoke();
                return UniTask.FromResult(InventoryOperationResult.CreateSuccess());
            }
            catch (Exception exception)
            {
                Debug.LogError($"Failed to add item: {exception}");
                return UniTask.FromResult(InventoryOperationResult.CreateFailure($"Internal error: {exception.Message}"));
            }
        }

        public UniTask<InventoryOperationResult> MoveItemAsync(
            InventoryItem item,
            Vector2Int newPosition,
            bool isRotated,
            CancellationToken token)
        {
            if (item == null)
                return UniTask.FromResult(InventoryOperationResult.CreateFailure("Item is null"));

            token.ThrowIfCancellationRequested();

            try
            {
                if (!CanPlaceItemAt(item, newPosition, isRotated, item))
                {
                    return UniTask.FromResult(InventoryOperationResult.CreateFailure(
                        "Cannot move item: target position is occupied"));
                }

                _gridService.ClearItem(item);
                _gridService.PlaceItem(item, newPosition, isRotated);

                item.Position = newPosition;
                item.IsRotated = isRotated;

                InventoryUpdated?.Invoke();
                return UniTask.FromResult(InventoryOperationResult.CreateSuccess(item));
            }
            catch (Exception exception)
            {
                Debug.LogError($"Failed to move item: {exception}");
                return UniTask.FromResult(InventoryOperationResult.CreateFailure($"Internal error: {exception.Message}"));
            }
        }

        public UniTask<InventoryOperationResult> SplitItemAsync(
            InventoryItem item,
            Vector2Int splitPosition,
            bool isRotated,
            CancellationToken token)
        {
            if (item == null)
                return UniTask.FromResult(InventoryOperationResult.CreateFailure("Item is null"));

            if (item.Amount < 2)
                return UniTask.FromResult(InventoryOperationResult.CreateFailure("Cannot split: item amount less than 2"));

            token.ThrowIfCancellationRequested();

            try
            {
                var splitSize = item.GetSize(isRotated);
                var originalSize = item.GetSize(item.IsRotated);

                if (_gridService.AreasOverlap(splitPosition, splitSize, item.Position, originalSize))
                {
                    return UniTask.FromResult(InventoryOperationResult.CreateFailure(
                        "Cannot split: overlap with original item"));
                }

                if (!_gridService.IsAreaFree(splitPosition, splitSize))
                {
                    return UniTask.FromResult(InventoryOperationResult.CreateFailure(
                        "Cannot split: target position is occupied"));
                }

                int moveAmount = item.Amount / 2;
                int keepAmount = item.Amount - moveAmount;

                item.Amount = keepAmount;

                var newItem = CreateInventoryItem(
                    _itemDatabase.GetItem(item.Id),
                    splitPosition,
                    moveAmount,
                    isRotated);

                _gridService.PlaceItem(newItem, splitPosition, isRotated);
                _model.AddItem(newItem);

                InventoryUpdated?.Invoke();
                return UniTask.FromResult(InventoryOperationResult.CreateSuccess(newItem));
            }
            catch (Exception exception)
            {
                Debug.LogError($"Failed to split item: {exception}");
                return UniTask.FromResult(InventoryOperationResult.CreateFailure($"Internal error: {exception.Message}"));
            }
        }

        public UniTask<InventoryOperationResult> RemoveItemAsync(
            InventoryItem item,
            CancellationToken token)
        {
            if (item == null)
                return UniTask.FromResult(InventoryOperationResult.CreateFailure("Item is null"));

            token.ThrowIfCancellationRequested();

            try
            {
                _gridService.ClearItem(item);
                _model.RemoveItem(item);

                InventoryUpdated?.Invoke();
                return UniTask.FromResult(InventoryOperationResult.CreateSuccess(item));
            }
            catch (Exception exception)
            {
                Debug.LogError($"Failed to remove item: {exception}");
                return UniTask.FromResult(InventoryOperationResult.CreateFailure($"Internal error: {exception.Message}"));
            }
        }

        public bool CanPlaceItem(ItemDefinition itemDefinition, Vector2Int position, bool isRotated)
        {
            if (itemDefinition == null)
                return false;

            var size = itemDefinition.CanRotate && isRotated
                ? new Vector2Int(itemDefinition.Height, itemDefinition.Width)
                : new Vector2Int(itemDefinition.Width, itemDefinition.Height);

            return _gridService.IsAreaFree(position, size);
        }

        public InventoryItem GetItemAtPosition(Vector2Int position)
        {
            foreach (var item in _model.Items)
            {
                var itemSize = item.GetSize(item.IsRotated);
                if (position.x >= item.Position.x && position.x < item.Position.x + itemSize.x &&
                    position.y >= item.Position.y && position.y < item.Position.y + itemSize.y)
                {
                    return item;
                }
            }

            return null;
        }

        private int TryStackExistingItems(ItemDefinition itemDefinition, int amount)
        {
            int remainingAmount = amount;

            foreach (var existingItem in _model.Items)
            {
                if (existingItem.Id == itemDefinition.Id && existingItem.Amount < existingItem.MaxStack)
                {
                    int space = existingItem.MaxStack - existingItem.Amount;
                    int toAdd = Mathf.Min(space, remainingAmount);

                    existingItem.Amount += toAdd;
                    remainingAmount -= toAdd;

                    if (remainingAmount <= 0)
                        break;
                }
            }

            return remainingAmount;
        }

        private InventoryOperationResult TryFindPlaceForItem(
            ItemDefinition itemDefinition,
            out Vector2Int position,
            out bool isRotated)
        {
            position = Vector2Int.zero;
            isRotated = false;

            var size = new Vector2Int(itemDefinition.Width, itemDefinition.Height);

            for (int y = 0; y <= _gridService.Height - size.y; y++)
            {
                for (int x = 0; x <= _gridService.Width - size.x; x++)
                {
                    var testPos = new Vector2Int(x, y);
                    if (_gridService.IsAreaFree(testPos, size))
                    {
                        position = testPos;
                        isRotated = false;
                        return InventoryOperationResult.CreateSuccess();
                    }
                }
            }

            if (itemDefinition.CanRotate)
            {
                var rotatedSize = new Vector2Int(size.y, size.x);
                for (int y = 0; y <= _gridService.Height - rotatedSize.y; y++)
                {
                    for (int x = 0; x <= _gridService.Width - rotatedSize.x; x++)
                    {
                        var testPos = new Vector2Int(x, y);
                        if (_gridService.IsAreaFree(testPos, rotatedSize))
                        {
                            position = testPos;
                            isRotated = true;
                            return InventoryOperationResult.CreateSuccess();
                        }
                    }
                }
            }

            return InventoryOperationResult.CreateFailure("No free space found");
        }

        private bool CanPlaceItemAt(InventoryItem item, Vector2Int position, bool isRotated, InventoryItem ignoreItem)
        {
            var size = item.GetSize(isRotated);
            return _gridService.IsAreaFree(position, size, ignoreItem);
        }

        private InventoryItem CreateInventoryItem(
            ItemDefinition itemDefinition,
            Vector2Int position,
            int amount,
            bool isRotated)
        {
            return new InventoryItem(
                itemDefinition.Id,
                position,
                new Vector2Int(itemDefinition.Width, itemDefinition.Height),
                itemDefinition.MaxStack,
                amount)
            {
                IsRotated = isRotated
            };
        }
        public IReadOnlyList<InventoryItem> GetAllItems()=> _model.Items;
    }
}
</file>

<file path="Assets/_Main/Scripts/InventoryModule/Runtime/Services/ItemProvider.cs">
using UnityEngine;

namespace InventoryModule
{
    public sealed class ItemProvider : IItemProvider
    {
        private readonly ItemDatabase _itemDatabase;

        public ItemProvider(ItemDatabase itemDatabase)
        {
            _itemDatabase = itemDatabase;
        }

        public ItemDefinition GetItemDefinition(string itemId)
            => _itemDatabase.GetItem(itemId);

        public bool TryGetItemDefinition(string itemId, out ItemDefinition itemDefinition)
            => _itemDatabase.TryGetItem(itemId, out itemDefinition);

        public Sprite GetItemIcon(string itemId)
            => _itemDatabase.GetItem(itemId)?.Icon;
    }
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/BuildingModule.asmdef">
{
    "name": "BuildingModule",
    "rootNamespace": "",
    "references": [
        "GUID:f51ebe6a0ceec4240a699833d6309b23",
        "GUID:2ee52fb8401542f4b618b9750c4c11fd",
        "GUID:0d8beb7f090555447a6cf5ce9e54dbb4"
    ],
    "includePlatforms": [],
    "excludePlatforms": [],
    "allowUnsafeCode": false,
    "overrideReferences": false,
    "precompiledReferences": [],
    "autoReferenced": true,
    "defineConstraints": [],
    "versionDefines": [],
    "noEngineReferences": false
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/Debug/GridOverlay.cs">
using UnityEngine;

namespace BuildingModule
{
    public class GridOverlay : MonoBehaviour
    {
        [SerializeField] private GridConfig gridConfig;

        private void OnDrawGizmos()
        {
            if (gridConfig == null)
                return;

            var tileSize = gridConfig.TileSize;
            Gizmos.color = Color.green;

            for (int x = 0; x < gridConfig.GridSizeX; x++)
            {
                for (int z = 0; z < gridConfig.GridSizeZ; z++)
                {
                    var tilePosition = new Vector3(x * tileSize, 0f, z * tileSize);
                    Gizmos.DrawWireCube(tilePosition, new(tileSize, 0f, tileSize));
                }
            }
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/Runtime/Commands/SelectBuildingCommand.cs">
using CommandsModule;
using Cysharp.Threading.Tasks;

namespace BuildingModule.Runtime.Commands
{
    public class SelectBuildingCommand : IAsyncCommand<BuildingName>
    {
        private readonly IPlacementService _placementService;

        public SelectBuildingCommand(IPlacementService placementService)
        {
            _placementService = placementService;
        }

        public UniTask<TaskResult> Execute(BuildingName name)
        {
            _placementService.SetBuilding(name);
            return UniTask.FromResult(TaskResult.Success);
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/Runtime/Controllers/ModeControllers.cs">
using UnityEngine;
using Zenject;

namespace BuildingModule
{
    public class ModeControllers : ITickable
    {
        private readonly IConstructionModeService _service;

        public ModeControllers(IConstructionModeService service)
        {
            _service = service;
        }

        public void Tick()
        {
            if (Input.GetKeyDown(KeyCode.Tab))
                _service.Toggle();
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/Runtime/Controllers/PlacementServiceController.cs">
using System;
using UnityEngine;
using Zenject;

namespace BuildingModule
{
    public class PlacementServiceController : IInitializable, ITickable, IDisposable
    {
        private readonly IConstructionModeService _constructionModeService;
        private readonly IPlacementService _placementService;

        private readonly Camera _camera;
        private readonly int _layers;

        private bool _isActive;

        public PlacementServiceController(IConstructionModeService constructionModeService,
            IPlacementService placementService,
            LayerMask layers)
        {
            _constructionModeService = constructionModeService;
            _placementService = placementService;
            _layers = layers;
            _camera = Camera.main;
        }

        public void Initialize() => _constructionModeService.ActiveChanged += OnActiveChanged;

        public void Tick()
        {
            if (!_isActive)
                return;

            if (Input.GetMouseButtonDown(1))
                _placementService.Cancel();

            if (!TryGetPosition(out var position))
                return;

            _placementService.UpdatePosition(position);

            if (Input.GetMouseButtonDown(0))
                _placementService.Build(position);
        }

        public void Dispose() => _constructionModeService.ActiveChanged -= OnActiveChanged;

        public void SetActive(bool value)
        {
            if (_isActive == value)
                return;

            _isActive = value;

            if (!_isActive)
                _placementService.Cancel();
        }

        private bool TryGetPosition(out Vector3 position)
        {
            position = default;

            var ray = _camera.ScreenPointToRay(Input.mousePosition);

            if (!Physics.Raycast(ray, out var hit, 100f, _layers))
                return false;

            position = hit.point;
            return true;
        }

        private void OnActiveChanged(bool isActive) => SetActive(isActive);
    }
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/Runtime/Core/BuildingFactory.cs">
using UnityEngine;

namespace BuildingModule
{
    public class BuildingFactory
    {
        private readonly BuildingCatalog _catalog;

        public BuildingFactory(BuildingCatalog catalog)
        {
            _catalog = catalog;
        }

        public BuildingView Create(BuildingName name, Vector3 position, Quaternion rotation)
        {
            if (!_catalog.TryGetConfig(name, out var config))
                return null;

            var view = Object.Instantiate(config.Prefab, position, rotation);
            view.SetBuildingName(name);
            return view;
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/Runtime/Core/CampGrid.cs">
using UnityEngine;

namespace BuildingModule
{
    public class CampGrid : IGrid
    {
        private readonly Tile[,] _tiles;
        private readonly float _tileSize;

        public CampGrid(Tile[,] tiles, float tileSize)
        {
            _tiles = tiles;
            _tileSize = tileSize;
        }

        public int RowsCount => _tiles.GetLength(0);
        public int ColumnsCount => _tiles.GetLength(1);
        public float TileSize => _tileSize;

        public bool TryGetNearestTile(Vector3 worldPosition, out Tile tile)
        {
            tile = null;

            var x = Mathf.RoundToInt(worldPosition.x / _tileSize);
            var z = Mathf.RoundToInt(worldPosition.z / _tileSize);

            if (x < 0 || z < 0 || x >= RowsCount || z >= ColumnsCount)
                return false;

            tile = _tiles[x, z];
            return true;
        }

        public static CampGrid CreateGrid(GridConfig gridConfig)
        {
            var gridSizeX = gridConfig.GridSizeX;
            var gridSizeZ = gridConfig.GridSizeZ;
            var tileSize = gridConfig.TileSize;

            var tiles = new Tile[gridSizeX, gridSizeZ];

            for (int x = 0; x < gridSizeX; x++)
            {
                for (int z = 0; z < gridSizeZ; z++)
                {
                    var worldPosition = new Vector3(x * tileSize, 0f, z * tileSize);
                    tiles[x, z] = new(worldPosition);
                }
            }

            return new(tiles, tileSize);
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/Runtime/Core/IGrid.cs">
using UnityEngine;

namespace BuildingModule
{
    public interface IGrid
    {
        int RowsCount { get; }
        int ColumnsCount { get; }

        float TileSize { get; }

        bool TryGetNearestTile(Vector3 worldPosition, out Tile tile);
    }
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/Runtime/Core/Tile.cs">
using UnityEngine;

namespace BuildingModule
{
    public class Tile
    {
        private readonly Vector3 _worldPosition;

        private bool _isOccupied;

        public Tile(Vector3 worldPosition)
        {
            _worldPosition = worldPosition;
        }

        public Vector3 WorldPosition => _worldPosition;
        public bool IsOccupied => _isOccupied;

        public void Occupy() => _isOccupied = true;
    }
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/Runtime/Data/BuildingCatalog.cs">
using System.Collections.Generic;
using System.Linq;
using UnityEngine;

namespace BuildingModule
{
    [CreateAssetMenu(fileName = "BuildingCatalog", menuName = "Game/Configs/Constructing/BuildingCatalog")]
    public class BuildingCatalog : ScriptableObject
    {
        //TODO: Replace with Odin serialized dictionary
        [SerializeField] private List<BuildingConfig> configs;

        private void OnValidate()
        {
            var success = configs.GroupBy(config => config.name)
                .All(group => group.Count() == 1);

            if (success)
                return;

            Debug.LogError($"Config with this {nameof(BuildingName)} exists");
            var last = configs[^1];
            configs.Remove(last);
        }

        public bool TryGetConfig(BuildingName buildingName, out BuildingConfig config)
        {
            config = configs.FirstOrDefault(config => config.Name == buildingName);
            return config;
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/Runtime/Data/BuildingConfig.cs">
using UnityEngine;

namespace BuildingModule
{
    [CreateAssetMenu(fileName = "BuildingConfig", menuName = "Game/Configs/Constructing/BuildingConfig")]
    public class BuildingConfig : ScriptableObject
    {
        [SerializeField] private BuildingName name;
        [SerializeField] private BuildingView prefab;

        public BuildingName Name => name;
        public BuildingView Prefab => prefab;
    }
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/Runtime/Data/BuildingName.cs">
namespace BuildingModule
{
    public enum BuildingName
    {
        None = 0,
        Bonfire,
    }
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/Runtime/Data/GridConfig.cs">
using UnityEngine;

namespace BuildingModule
{
    [CreateAssetMenu(fileName = "GridConfig", menuName = "Game/Configs/Constructing/GridConfig")]
    public class GridConfig : ScriptableObject
    {
        [SerializeField] private int gridSizeX = 10;
        [SerializeField] private int gridSizeZ = 10;
        [SerializeField] private float tileSize = 1f;

        public int GridSizeX => gridSizeX;
        public int GridSizeZ => gridSizeZ;
        public float TileSize => tileSize;
    }
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/Runtime/Installers/BuildingModuleInstaller.cs">
using BuildingModule.Runtime.Commands;
using UnityEngine;
using Zenject;

namespace BuildingModule
{
    public class BuildingModuleInstaller : MonoInstaller
    {
        [SerializeField] private GridConfig gridConfig;
        [SerializeField] private BuildingCatalog buildingCatalog;

        [SerializeField] private LayerMask groundMask;

        [SerializeField] private GridView gridView;
        [SerializeField] private GameObject buildPanel;

        public override void InstallBindings()
        {
            var grid = CampGrid.CreateGrid(gridConfig);

            Container.Bind<GridConfig>().FromInstance(gridConfig).AsSingle();
            Container.Bind<BuildingCatalog>().FromInstance(buildingCatalog).AsSingle();

            Container.Bind<IGrid>().FromInstance(grid).AsSingle();

            Container.Bind<IPlacementService>().To<PlacementService>().AsSingle();
            Container.Bind<IPreviewService>().To<PreviewService>().AsSingle();
            Container.Bind<BuildingFactory>().AsSingle();

            Container.Bind<IConstructionModeService>().To<ConstructionModeService>().AsSingle();

            Container.BindInterfacesTo<PlacementServiceController>().AsSingle().WithArguments(groundMask).NonLazy();
            Container.BindInterfacesTo<ModeControllers>().AsSingle().NonLazy();

            Container.Bind<SelectBuildingCommand>().AsSingle().NonLazy();

            Container.BindInterfacesTo<ConstructionModePresenter>().AsSingle().WithArguments(buildPanel, gridView).NonLazy();
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/Runtime/Presenters/ConstructionModePresenter.cs">
using System;
using UnityEngine;
using Zenject;

namespace BuildingModule
{
    public class ConstructionModePresenter : IInitializable, IDisposable
    {
        private readonly IConstructionModeService _service;

        private readonly GameObject _constructionPanel;
        private readonly GridView _gridView;

        public ConstructionModePresenter(IConstructionModeService service,
            GameObject constructionPanel,
            GridView gridView)
        {
            _service = service;
            _constructionPanel = constructionPanel;
            _gridView = gridView;
        }

        public void Initialize() => _service.ActiveChanged += OnActiveChanged;

        public void Dispose() => _service.ActiveChanged -= OnActiveChanged;

        private void OnActiveChanged(bool isActive)
        {
            _gridView.gameObject.SetActive(isActive);
            _constructionPanel.SetActive(isActive);
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/Runtime/Services/ConstructionModeService.cs">
using System;

namespace BuildingModule
{
    public class ConstructionModeService : IConstructionModeService
    {
        private bool _isActive;

        public event Action<bool> ActiveChanged;

        public void Toggle() => SetActive(!_isActive);

        public void SetActive(bool value)
        {
            if (_isActive == value)
                return;

            _isActive = value;
            ActiveChanged?.Invoke(value);
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/Runtime/Services/IConstructionModeService.cs">
using System;

namespace BuildingModule
{
    public interface IConstructionModeService
    {
        event Action<bool> ActiveChanged;

        void Toggle();

        void SetActive(bool value);
    }
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/Runtime/Services/IPlacementService.cs">
using UnityEngine;

namespace BuildingModule
{
    public interface IPlacementService
    {
        BuildingName CurrentBuilding { get; }

        void SetBuilding(BuildingName value);
        void UpdatePosition(Vector3 worldPosition);
        bool Build(Vector3 worldPosition);
        void Cancel();
    }
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/Runtime/Services/IPreviewService.cs">
using UnityEngine;

namespace BuildingModule
{
    public interface IPreviewService
    {
        void SetPreview(BuildingName value);
        void UpdatePreview(Vector3 position, bool isOccupied);
        void Cancel();
    }
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/Runtime/Services/PlacementService.cs">
using UnityEngine;

namespace BuildingModule
{
    public class PlacementService : IPlacementService
    {
        private readonly IGrid _grid;

        private readonly IPreviewService _previewService;
        private readonly BuildingFactory _factory;

        private BuildingName _currentBuilding;

        public PlacementService(IGrid grid, IPreviewService previewService, BuildingFactory factory)
        {
            _grid = grid;
            _previewService = previewService;
            _factory = factory;
        }

        public BuildingName CurrentBuilding => _currentBuilding;

        public void SetBuilding(BuildingName value)
        {
            if (_currentBuilding == value)
                return;

            _currentBuilding = value;
            _previewService.SetPreview(value);
        }

        public void UpdatePosition(Vector3 worldPosition)
        {
            if (_currentBuilding == BuildingName.None)
                return;

            if (!_grid.TryGetNearestTile(worldPosition, out var tile))
                return;

            _previewService.UpdatePreview(tile.WorldPosition, tile.IsOccupied);
        }

        public bool Build(Vector3 worldPosition)
        {
            if (_currentBuilding == BuildingName.None)
                return false;

            if (!_grid.TryGetNearestTile(worldPosition, out var tile))
                return false;

            if (tile.IsOccupied)
                return false;

            _factory.Create(_currentBuilding, tile.WorldPosition, Quaternion.identity);
            tile.Occupy();
            Cancel();
            return true;
        }

        public void Cancel() => SetBuilding(BuildingName.None);
    }
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/Runtime/Services/PreviewService.cs">
using UnityEngine;

namespace BuildingModule
{
    public class PreviewService : IPreviewService
    {
        private readonly BuildingFactory _factory;

        private BuildingView _preview;

        public PreviewService(BuildingFactory factory)
        {
            _factory = factory;
        }

        public void SetPreview(BuildingName value)
        {
            if (_preview?.Name == value)
                return;

            if (_preview)
                DestroyPreview();

            if (value == BuildingName.None)
                return;

            _preview = _factory.Create(value, Vector3.zero, Quaternion.identity);

            if (!_preview)
                return;

            UpdateMaterial(false);
        }

        public void UpdatePreview(Vector3 position, bool isOccupied)
        {
            position.y += 0.1f;
            _preview.transform.position = position;
            UpdateMaterial(isOccupied);
        }

        public void Cancel() => DestroyPreview();

        private void DestroyPreview()
        {
            Object.Destroy(_preview.gameObject);
            _preview = null;
        }

        private void UpdateMaterial(bool isOccupied)
        {
            var color = isOccupied ? Color.red : Color.green;
            _preview.MeshRenderer.material.color = color;
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/Runtime/View/BuildingView.cs">
using UnityEngine;

namespace BuildingModule
{
    public class BuildingView : MonoBehaviour
    {
        [SerializeField] private MeshRenderer meshRenderer;

        private BuildingName _name;

        public MeshRenderer MeshRenderer => meshRenderer;
        public BuildingName Name => _name;

        public void SetBuildingName(BuildingName value) => _name = value;
    }
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/Runtime/View/Buttons/SelectBuildingButton.cs">
using BuildingModule.Runtime.Commands;
using CommandsModule;

namespace BuildingModule
{
    public class SelectBuildingButton : ExecuteCommandButton<SelectBuildingCommand, BuildingName>
    {
    }
}
</file>

<file path="Assets/_Main/Scripts/BuildingModule/Runtime/View/GridView.cs">
using System.Collections.Generic;
using UnityEngine;
using Zenject;

namespace BuildingModule
{
    public sealed class GridView : MonoBehaviour
    {
        [SerializeField] private float yOffset = 0.02f;
        [SerializeField] private float lineWidth = 0.03f;

        private readonly List<LineRenderer> _lines = new();

        [Inject]
        public void Construct(IGrid grid)
        {
            BuildLines(grid);
        }

        private void BuildLines(IGrid grid)
        {
            ClearLines();

            var gridSizeX = grid.RowsCount;
            var gridSizeZ = grid.ColumnsCount;

            var tileSize = grid.TileSize;

            for (int x = 0; x <= gridSizeX; x++)
            {
                var start = new Vector3(x * tileSize, yOffset, 0f);
                var end = new Vector3(x * tileSize, yOffset, gridSizeZ * tileSize);
                _lines.Add(CreateLine(start, end));
            }

            for (int z = 0; z <= gridSizeZ; z++)
            {
                var start = new Vector3(0f, yOffset, z * tileSize);
                var end = new Vector3(gridSizeX * tileSize, yOffset, z * tileSize);
                _lines.Add(CreateLine(start, end));
            }
        }

        private LineRenderer CreateLine(Vector3 start, Vector3 end)
        {
            var lineObject = new GameObject("GridLine");
            lineObject.transform.SetParent(transform, false);

            var line = lineObject.AddComponent<LineRenderer>();
            line.useWorldSpace = false;
            line.positionCount = 2;
            line.SetPosition(0, start);
            line.SetPosition(1, end);
            line.widthMultiplier = lineWidth;
            line.material = new(Shader.Find("Sprites/Default"));

            return line;
        }

        private void ClearLines()
        {
            for (int i = 0; i < _lines.Count; i++)
            {
                if (_lines[i] != null)
                    Destroy(_lines[i].gameObject);
            }

            _lines.Clear();
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/CameraModule/CameraModule.asmdef">
{
    "name": "CameraModule",
    "rootNamespace": "",
    "references": [
        "GUID:db07fce7139c47e5b1b441a134892332",
        "GUID:0d8beb7f090555447a6cf5ce9e54dbb4"
    ],
    "includePlatforms": [],
    "excludePlatforms": [],
    "allowUnsafeCode": false,
    "overrideReferences": false,
    "precompiledReferences": [],
    "autoReferenced": true,
    "defineConstraints": [],
    "versionDefines": [],
    "noEngineReferences": false
}
</file>

<file path="Assets/_Main/Scripts/CameraModule/Runtime/BuildingMode/Controllers/BuildingModeCameraController.cs">
using System;
using BuildingModule;
using UnityEngine;
using Zenject;

namespace CameraModule
{
    public class BuildingModeCameraController : IInitializable, ILateTickable, IDisposable
    {
        private readonly IBuildingModeCameraService _cameraService;
        private readonly IConstructionModeService _modeService;

        private bool _isActive;

        public BuildingModeCameraController(IConstructionModeService modeService, IBuildingModeCameraService cameraService)
        {
            _modeService = modeService;
            _cameraService = cameraService;
        }

        public void Initialize() => _modeService.ActiveChanged += SetActive;

        public void LateTick()
        {
            if (!_isActive)
                return;

            Move();
            Rotate();
        }

        public void Dispose() => _modeService.ActiveChanged -= SetActive;

        public void SetActive(bool value)
        {
            if (_isActive == value)
                return;

            _isActive = value;

            if (!_isActive)
                _cameraService.Reset();
        }

        private void Move()
        {
            var direction = Vector3.right * Input.GetAxis("Horizontal") + Vector3.forward * Input.GetAxis("Vertical");
            _cameraService.Move(direction);
        }

        private void Rotate()
        {
            if (!Input.GetMouseButton(2))
                return;

            var delta = Input.GetAxisRaw("Mouse X");
            _cameraService.Rotate(delta);
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/CameraModule/Runtime/BuildingMode/Core/BuildingModeCameraService.cs">
using UnityEngine;

namespace CameraModule
{
    public class BuildingModeCameraService : IBuildingModeCameraService
    {
        private readonly Transform _transform;

        private readonly float _moveSpeed;
        private readonly float _rotationSpeed;

        private readonly float _minHeight;
        private readonly float _maxHeight;

        private readonly Vector3 _startPosition;
        private readonly Quaternion _startRotation;

        private float _angle;

        public BuildingModeCameraService(float moveSpeed, float rotationSpeed, float minHeight, float maxHeight)
        {
            _moveSpeed = moveSpeed;
            _rotationSpeed = rotationSpeed;

            _minHeight = minHeight;
            _maxHeight = maxHeight;

            _transform = Camera.main!.transform;
            _transform.GetPositionAndRotation(out _startPosition, out _startRotation);
        }

        public void Move(Vector3 direction)
        {
            var translation = direction * (_moveSpeed * Time.deltaTime);
            translation.y = 0;
            _transform.Translate(translation);
            Clamp();
        }

        public void Rotate(float delta)
        {
            var angle = delta * _rotationSpeed;
            _angle += angle;
            _transform.rotation = Quaternion.Euler(_startRotation.eulerAngles.x, _angle, 0f);
        }

        public void Reset() => _transform.SetPositionAndRotation(_startPosition, _startRotation);

        private void Clamp()
        {
            var position = _transform.position;
            position.y = Mathf.Clamp(position.y, _minHeight, _maxHeight);
            _transform.position = position;
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/CameraModule/Runtime/BuildingMode/Core/IBuildingModeCameraService.cs">
using UnityEngine;

namespace CameraModule
{
    public interface IBuildingModeCameraService
    {
        void Move(Vector3 direction);
        void Rotate(float delta);
        void Reset();
    }
}
</file>

<file path="Assets/_Main/Scripts/CameraModule/Runtime/Installers/CameraInstaller.cs">
using UnityEngine;
using Zenject;

namespace CameraModule
{
    public class CameraInstaller : MonoInstaller
    {
        [SerializeField] private float moveSpeed = 5f;
        [SerializeField] private float rotationSpeed = 2f;
        [SerializeField] private float minHeight = 1f;
        [SerializeField] private float maxHeight = 10f;

        public override void InstallBindings()
        {
            Container.Bind<IBuildingModeCameraService>()
                .To<BuildingModeCameraService>()
                .AsSingle()
                .WithArguments(moveSpeed, rotationSpeed, minHeight, maxHeight);

            Container.BindInterfacesTo<BuildingModeCameraController>()
                .AsSingle()
                .NonLazy();
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/CommandsModule/CommandsModule.asmdef">
{
    "name": "CommandsModule",
    "rootNamespace": "",
    "references": [
        "GUID:f51ebe6a0ceec4240a699833d6309b23"
    ],
    "includePlatforms": [],
    "excludePlatforms": [],
    "allowUnsafeCode": false,
    "overrideReferences": false,
    "precompiledReferences": [],
    "autoReferenced": true,
    "defineConstraints": [],
    "versionDefines": [],
    "noEngineReferences": false
}
</file>

<file path="Assets/_Main/Scripts/CommandsModule/Runtime/Core/IAsyncCommand.cs">
using Cysharp.Threading.Tasks;

namespace CommandsModule
{
    public interface IAsyncCommand
    {
        UniTask<TaskResult> Execute();
    }

    public interface IAsyncCommand<TArgument>
    {
        UniTask<TaskResult> Execute(TArgument argument1);
    }

    public interface IAsyncCommand<TArgument1, TArgument2>
    {
        UniTask<TaskResult> Execute(TArgument1 argument1, TArgument2 argument2);
    }

    public interface IAsyncCommand<TArgument1, TArgument2, TArgument3>
    {
        UniTask<TaskResult> Execute(TArgument1 argument1, TArgument2 argument2, TArgument3 argument3);
    }

    public interface IAsyncCommand<TArgument1, TArgument2, TArgument3, TArgument4>
    {
        UniTask<TaskResult> Execute(TArgument1 argument1, TArgument2 argument2, TArgument3 argument3, TArgument4 argument4);
    }

    public interface IAsyncCommand<TArgument1, TArgument2, TArgument3, TArgument4, TArgument5>
    {
        UniTask<TaskResult> Execute(TArgument1 argument1, TArgument2 argument2, TArgument3 argument3, TArgument4 argument4, TArgument5 argument5);
    }
}
</file>

<file path="Assets/_Main/Scripts/CommandsModule/Runtime/Core/TaskResult.cs">
namespace CommandsModule
{
    public enum TaskResult
    {
        Success = 0,
        Failure,
        Running,
        Interrupted,
        NotStarted,
    }
}
</file>

<file path="Assets/_Main/Scripts/CommandsModule/Runtime/View/ExecuteCommandButton.cs">
using UnityEngine;
using UnityEngine.UI;
using Zenject;

namespace CommandsModule
{
    public abstract class ExecuteCommandButton<TAsyncCommand> : MonoBehaviour where TAsyncCommand : IAsyncCommand
    {
        [SerializeField] private Button button;

        [Inject] private TAsyncCommand _command;

        protected Button Button => button;

        private void OnEnable() => button.onClick.AddListener(OnButtonClick);

        private void OnDisable() => button.onClick.RemoveListener(OnButtonClick);

        private void OnValidate() => button ??= GetComponent<Button>();

        protected virtual void OnButtonClick() => _command.Execute();
    }


    public abstract class ExecuteCommandButton<TAsyncCommand, TArgument> : MonoBehaviour
        where TAsyncCommand : IAsyncCommand<TArgument>
    {
        [SerializeField] private Button button;
        [SerializeField] private TArgument argument;

        [Inject] private TAsyncCommand _command;

        protected Button Button => button;
        protected TArgument Argument => argument;

        private void OnEnable() => button.onClick.AddListener(OnButtonClick);

        private void OnDisable() => button.onClick.RemoveListener(OnButtonClick);

        private void OnValidate() => button ??= GetComponent<Button>();

        protected virtual void OnButtonClick() => _command.Execute(argument);
    }

    public abstract class ExecuteCommandButton<TAsyncCommand, TArgument1, TArgument2> : MonoBehaviour
        where TAsyncCommand : IAsyncCommand<TArgument1, TArgument2>
    {
        [SerializeField] private Button button;
        [SerializeField] private TArgument1 argument1;
        [SerializeField] private TArgument2 argument2;

        [Inject] private TAsyncCommand _command;

        protected Button Button => button;
        protected TArgument1 Argument1 => argument1;
        protected TArgument2 Argument2 => argument2;

        private void OnEnable() => button.onClick.AddListener(OnButtonClick);

        private void OnDisable() => button.onClick.RemoveListener(OnButtonClick);

        private void OnValidate() => button ??= GetComponent<Button>();

        protected virtual void OnButtonClick() => _command.Execute(argument1, argument2);
    }

    public abstract class ExecuteCommandButton<TAsyncCommand, TArgument1, TArgument2, TArgument3> : MonoBehaviour
        where TAsyncCommand : IAsyncCommand<TArgument1, TArgument2, TArgument3>
    {
        [SerializeField] private Button button;
        [SerializeField] private TArgument1 argument1;
        [SerializeField] private TArgument2 argument2;
        [SerializeField] private TArgument3 argument3;

        [Inject] private TAsyncCommand _command;

        protected Button Button => button;
        protected TArgument1 Argument1 => argument1;
        protected TArgument2 Argument2 => argument2;
        protected TArgument3 Argument3 => argument3;

        private void OnEnable() => button.onClick.AddListener(OnButtonClick);

        private void OnDisable() => button.onClick.RemoveListener(OnButtonClick);

        private void OnValidate() => button ??= GetComponent<Button>();

        protected virtual void OnButtonClick() => _command.Execute(argument1, argument2, argument3);
    }

    public abstract class ExecuteCommandButton<TAsyncCommand, TArgument1, TArgument2, TArgument3, TArgument4> : MonoBehaviour
        where TAsyncCommand : IAsyncCommand<TArgument1, TArgument2, TArgument3, TArgument4>
    {
        [SerializeField] private Button button;
        [SerializeField] private TArgument1 argument1;
        [SerializeField] private TArgument2 argument2;
        [SerializeField] private TArgument3 argument3;
        [SerializeField] private TArgument4 argument4;

        [Inject] private TAsyncCommand _command;

        protected Button Button => button;
        protected TArgument1 Argument1 => argument1;
        protected TArgument2 Argument2 => argument2;
        protected TArgument3 Argument3 => argument3;
        protected TArgument4 Argument4 => argument4;

        private void OnEnable() => button.onClick.AddListener(OnButtonClick);

        private void OnDisable() => button.onClick.RemoveListener(OnButtonClick);

        private void OnValidate() => button ??= GetComponent<Button>();

        protected virtual void OnButtonClick() => _command.Execute(argument1, argument2, argument3, argument4);
    }

    public abstract class ExecuteCommandButton<TAsyncCommand, TArgument1, TArgument2, TArgument3, TArgument4, TArgument5> : MonoBehaviour
        where TAsyncCommand : IAsyncCommand<TArgument1, TArgument2, TArgument3, TArgument4, TArgument5>
    {
        [SerializeField] private Button button;
        [SerializeField] private TArgument1 argument1;
        [SerializeField] private TArgument2 argument2;
        [SerializeField] private TArgument3 argument3;
        [SerializeField] private TArgument4 argument4;
        [SerializeField] private TArgument5 argument5;

        [Inject] private TAsyncCommand _command;

        protected Button Button => button;
        protected TArgument1 Argument1 => argument1;
        protected TArgument2 Argument2 => argument2;
        protected TArgument3 Argument3 => argument3;
        protected TArgument4 Argument4 => argument4;
        protected TArgument5 Argument5 => argument5;

        private void OnEnable() => button.onClick.AddListener(OnButtonClick);

        private void OnDisable() => button.onClick.RemoveListener(OnButtonClick);

        private void OnValidate() => button ??= GetComponent<Button>();

        protected virtual void OnButtonClick() => _command.Execute(argument1, argument2, argument3, argument4, argument5);
    }
}
</file>

<file path="Assets/_Main/Scripts/ComponentsModule/Runtime/Crouch/CrouchComponent.cs">
using UnityEngine;

namespace ComponentsModule
{
    public class CrouchComponent : ICrouchComponent
    {
        private readonly CharacterController _characterController;
        private readonly Transform _cameraRoot;
        private readonly CrouchData _data;

        public CrouchComponent(CharacterController characterController, Transform cameraRoot, CrouchData data)
        {
            _characterController = characterController;
            _cameraRoot = cameraRoot;
            _data = data;
        }

        public void Crouch(bool isCrouching)
        {
            var targetHeight = isCrouching ? _data.CrouchHeight : _data.StandHeight;
            var targetEyeHeight = isCrouching ? _data.EyeHeightCrouching : _data.EyeHeightStanding;

            var height = Mathf.Lerp(_characterController.height, targetHeight, Time.deltaTime * _data.CrouchSpeed);
            _characterController.height = height;
            _characterController.center = Vector3.up * height / 2f;

            var currentPosition = _cameraRoot.localPosition;
            var targetPosition = new Vector3(0, targetEyeHeight, 0);

            var position = Vector3.Lerp(currentPosition, targetPosition, Time.deltaTime * _data.CrouchSpeed);
            _cameraRoot.localPosition = position;
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/ComponentsModule/Runtime/Crouch/CrouchData.cs">
using System;
using UnityEngine;

namespace ComponentsModule
{
    [Serializable]
    public class CrouchData
    {
        [SerializeField] private float crouchHeight = 1.1f;
        [SerializeField] private float standHeight = 1.5f;

        [SerializeField] private float eyeHeightCrouching = 0.8f;
        [SerializeField] private float eyeHeightStanding = 1.2f;

        [SerializeField] private float crouchSpeed = 10;

        public float CrouchHeight => crouchHeight;
        public float StandHeight => standHeight;

        public float EyeHeightCrouching => eyeHeightCrouching;
        public float EyeHeightStanding => eyeHeightStanding;

        public float CrouchSpeed => crouchSpeed;
    }
}
</file>

<file path="Assets/_Main/Scripts/ComponentsModule/Runtime/Crouch/ICrouchComponent.cs">
namespace ComponentsModule
{
    public interface ICrouchComponent
    {
        void Crouch(bool isCrouching);
    }
}
</file>

<file path="Assets/_Main/Scripts/ComponentsModule/Runtime/Health/DamageHandler.cs">
using UnityEngine;
using Zenject;

namespace ComponentsModule
{
    public class DamageHandler : MonoBehaviour, IDamageable
    {
        private IDamageable _health;

        [Inject]
        public void Construct(IDamageable health)
        {
            _health = health;
        }

        public bool IsAlive => _health != null && _health.IsAlive;

        public void TakeDamage(float amount, Vector3? hitPoint = null, Vector3? force = null)
            => _health?.TakeDamage(amount, hitPoint, force);
    }
}
</file>

<file path="Assets/_Main/Scripts/ComponentsModule/Runtime/Health/HealthComponent.cs">
using System;
using UnityEngine;

namespace ComponentsModule
{
    public class HealthComponent : IHealthComponent
    {
        private readonly float _maxHealth;

        private float _currentHealth;

        public event Action<float, float> HealthChanged;
        public event Action<Vector3?, Vector3?> DamageTaken;
        public event Action Died;

        public float MaxHealth => _maxHealth;
        public float CurrentHealth => _currentHealth;
        public bool IsAlive => _currentHealth > 0;

        public HealthComponent(float maxHealth)
        {
            _maxHealth = maxHealth;
            _currentHealth = maxHealth;
        }

        public void TakeDamage(float amount, Vector3? hitPoint = null, Vector3? force = null)
        {
            if (!IsAlive)
                return;

            _currentHealth -= amount;
            _currentHealth = Mathf.Max(_currentHealth, 0);

            HealthChanged?.Invoke(_currentHealth, _maxHealth);
            DamageTaken?.Invoke(hitPoint, force);

            if (_currentHealth <= 0)
                Died?.Invoke();
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/ComponentsModule/Runtime/Health/IDamageable.cs">
using UnityEngine;

namespace ComponentsModule
{
    public interface IDamageable
    {
        bool IsAlive { get; }

        void TakeDamage(float amount, Vector3? hitPoint = null, Vector3? force = null);
    }
}
</file>

<file path="Assets/_Main/Scripts/ComponentsModule/Runtime/Health/IHealthComponent.cs">
using System;
using UnityEngine;

namespace ComponentsModule
{
    public interface IHealthComponent : IDamageable
    {
        event Action<float, float> HealthChanged;
        event Action<Vector3?, Vector3?> DamageTaken;
        event Action Died;

        float MaxHealth { get; }
        float CurrentHealth { get; }
    }
}
</file>

<file path="Assets/_Main/Scripts/ComponentsModule/Runtime/Lean/ILeanComponent.cs">
namespace ComponentsModule
{
    public interface ILeanComponent
    {
        void Lean(float targetLean);
    }
}
</file>

<file path="Assets/_Main/Scripts/ComponentsModule/Runtime/Lean/LeanComponent.cs">
using UnityEngine;

namespace ComponentsModule
{
    public class LeanComponent : ILeanComponent
    {
        private readonly Transform _cameraRoot;

        private readonly float _leanAngle;
        private readonly float _leanOffset;
        private readonly float _leanSpeed;

        private float _currentLean;

        public LeanComponent(Transform cameraRoot, float leanAngle, float leanOffset, float leanSpeed)
        {
            _cameraRoot = cameraRoot;
            _leanAngle = leanAngle;
            _leanOffset = leanOffset;
            _leanSpeed = leanSpeed;
        }

        public void Lean(float targetLean)
        {
            _currentLean = Mathf.Lerp(_currentLean, targetLean, Time.deltaTime * _leanSpeed);

            var rotation = Quaternion.Euler(0, 0, -_currentLean * _leanAngle);
            _cameraRoot.localRotation = rotation;

            var position = _cameraRoot.localPosition;
            position.x = _currentLean * _leanOffset;
            _cameraRoot.localPosition = position;
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/ComponentsModule/Runtime/Movement/IMoveComponent.cs">
using UnityEngine;

namespace ComponentsModule
{
    public interface IMoveComponent
    {
        void Move(Vector2 direction, bool jumpe);
        void SetSpeed(float value);
    }
}
</file>

<file path="Assets/_Main/Scripts/ComponentsModule/Runtime/Movement/MoveComponent.cs">
using UnityEngine;

namespace ComponentsModule
{
    public class MoveComponent : IMoveComponent
    {
        private const float GroundingForce = -2f;

        private readonly CharacterController _characterController;
        private readonly Transform _transform;

        private readonly float _jumpHeight;
        private readonly float _gravity;

        private float _speed;
        private float _verticalVelocity;

        public MoveComponent(CharacterController characterController, float speed, float jumpHeight, float gravity)
        {
            _characterController = characterController;
            _transform = characterController.transform;

            _jumpHeight = jumpHeight;
            _gravity = gravity;

            _speed = speed;
        }

        public void Move(Vector2 direction, bool isJumping)
        {
            var movement = direction.x * _transform.right + direction.y * _transform.forward;
            movement *= _speed;

            _verticalVelocity = CalculateVerticalVelocity(isJumping);
            movement.y = _verticalVelocity;

            _characterController.Move(movement * Time.deltaTime);
        }

        public void SetSpeed(float value)
        {
            _speed = value;
        }

        private float CalculateVerticalVelocity(bool isJumping)
        {
            var verticalVelocity = _verticalVelocity;
            var isGrounded = _characterController.isGrounded;

            if (isGrounded)
            {
                if (isJumping)
                {
                    verticalVelocity = Mathf.Sqrt(_jumpHeight * GroundingForce * _gravity);
                }
                else if (verticalVelocity < 0)
                {
                    verticalVelocity = GroundingForce;
                }
            }
            else
            {
                verticalVelocity += _gravity * Time.deltaTime;
            }

            return verticalVelocity;
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/ComponentsModule/Runtime/Rotation/IRotationComponent.cs">
using UnityEngine;

namespace ComponentsModule
{
    public interface IRotationComponent
    {
        void Rotate(Vector2 direction);
    }
}
</file>

<file path="Assets/_Main/Scripts/ComponentsModule/Runtime/Rotation/RotationComponent.cs">
using UnityEngine;

namespace ComponentsModule
{
    public class RotationComponent : IRotationComponent
    {
        private readonly Transform _transform;
        private readonly Transform _cameraTransform;

        private readonly float _mouseSensitivity;
        private readonly float _lookXLimit;

        private float _rotationX;
        private float _rotationY;

        public RotationComponent(Transform transform, float mouseSensitivity, float lookXLimit)
        {
            _transform = transform;
            _cameraTransform = Camera.main!.transform;

            _rotationY = _transform.eulerAngles.y;

            _mouseSensitivity = mouseSensitivity;
            _lookXLimit = lookXLimit;
        }

        public void Rotate(Vector2 direction)
        {
            _rotationX += -direction.y * _mouseSensitivity;
            _rotationX = Mathf.Clamp(_rotationX, -_lookXLimit, _lookXLimit);

            _rotationY += direction.x * _mouseSensitivity;

            _cameraTransform.localRotation = Quaternion.Euler(_rotationX, 0, 0);
            _transform.rotation = Quaternion.Euler(0, _rotationY, 0);
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/EnemyModule/Configs/EnemyConfig.cs">
using UnityEngine;

namespace EnemyModule
{
    [CreateAssetMenu(fileName = "EnemyConfig", menuName = "Configs/Enemy")]
    public class EnemyConfig : ScriptableObject
    {
        [Header("Stats")]
        public float MaxHealth = 100f;

        [Header("Sensing")]
        public float SightDistance = 25f;
        public float ViewAngle = 120f;
        public LayerMask ViewMask;
        public float MemoryTime = 10f;

        [Header("Combat")]
        public float AttackRange = 10f;
        public float FireRate = 1f;
        public int MaxAmmo = 10;
        public float ReloadTime = 3f;
        public float BulletSpeed = 30f;
        public float Damage = 10f;

        [Header("Patrol")]
        public float PatrolWaitTime = 3f;
        public float LookInterval = 2f;
        public float LookTurnSpeed = 2f;
        public float LookAngleRange = 60f;
    }
}
</file>

<file path="Assets/_Main/Scripts/EnemyModule/Controllers/EnemyController.cs">
using Cysharp.Threading.Tasks;
using System;
using System.Threading;
using PlayerModule;
using UnityEngine;
using Zenject;

namespace EnemyModule
{
    public class EnemyController : IInitializable, ITickable, IDisposable
    {
        private readonly PlayerProvider _player;
        private readonly EnemyConfig _config;
        private readonly EnemyModel _model;
        private readonly EnemyView _view;

        private CancellationTokenSource _cancellationToken;

        private Transform _playerTransform;
        private float _nextFireTime;
        private float _chaseTimer;
        private int _patrolIndex = 0;
        private bool _isActionBusy;

        public EnemyController(EnemyConfig config,
            EnemyModel model,
            EnemyView view,
            PlayerProvider player)
        {
            _player = player;
            _config = config;
            _model = model;
            _view = view;
        }

        public void Initialize()
        {
            var playerTransform = _player.Get<Transform>();
            _cancellationToken = new();

            if (playerTransform)
                _playerTransform = playerTransform.transform;

            _view.Initialize(_config);
            _model.Initialize(_config.MaxAmmo, _view.transform.position);

            _view.Health.DamageTaken += OnTakeDamage;
            _view.Health.Died += OnDeath;
        }

        public void Dispose()
        {
            if (_cancellationToken != null)
            {
                _cancellationToken.Cancel();
                _cancellationToken.Dispose();
            }

            if (_view && _view.Health != null)
            {
                _view.Health.DamageTaken -= OnTakeDamage;
                _view.Health.Died -= OnDeath;
            }
        }

        public void Tick()
        {
            if (!_view || _view.Health is not { IsAlive: true })
                return;

            _view.UpdateAnimator(_view.Velocity);

            if (_isActionBusy)
                return;

            _view.UpdateAnimator(_view.Velocity);

            switch (_model.CurrentState)
            {
                case AIState.Patrol:
                    UpdatePatrol();
                    break;

                case AIState.Chase:
                    UpdateChase();
                    break;

                case AIState.Attack:
                    UpdateAttack();
                    break;

                case AIState.Reload:
                    break;
            }
        }

        private void UpdatePatrol()
        {
            if (TrySpotPlayer())
                return;

            if (_view.PatrolPoints.Length == 0)
                return;

            //FIXME: Magic numbers
            if (!_view.IsPathPending && _view.RemainingDistance < 0.5f)
                PatrolWaitRoutine().Forget();
        }

        private async UniTaskVoid PatrolWaitRoutine()
        {
            _isActionBusy = true;
            _view.StopMove();

            float totalWaitTime = _config.PatrolWaitTime;
            float timer = 0;
            float nextLookTime = 0;

            while (timer < totalWaitTime)
            {
                timer += Time.deltaTime;
                ;

                if (TrySpotPlayer())
                {
                    _isActionBusy = false;
                    return;
                }

                if (timer >= nextLookTime)
                {
                    nextLookTime = timer + UnityEngine.Random.Range(1.5f, 3f);

                    float randomAngle = UnityEngine.Random.Range(-_config.LookAngleRange, _config.LookAngleRange);

                    RotateToAngleRoutine(randomAngle).Forget();
                }

                await UniTask.Yield(PlayerLoopTiming.Update, _cancellationToken.Token).SuppressCancellationThrow();

                if (!_view || !_view.Health.IsAlive)
                    return;
            }

            _patrolIndex = (_patrolIndex + 1) % _view.PatrolPoints.Length;
            _view.MoveTo(_view.PatrolPoints[_patrolIndex].position);

            _isActionBusy = false;
        }

        private async UniTaskVoid RotateToAngleRoutine(float angleOffset)
        {
            Quaternion startRot = _view.transform.rotation;
            Quaternion targetRot = startRot * Quaternion.Euler(0, angleOffset, 0);

            //Fixme: MagicNumbers
            float lerpFactor = 0;
            float duration = 1.0f;

            while (lerpFactor < 1f)
            {
                if (_cancellationToken.IsCancellationRequested || !_view.Health.IsAlive)
                    return;

                if (_model.CurrentState != AIState.Patrol)
                    return;

                lerpFactor += Time.deltaTime / duration;

                _view.transform.rotation = Quaternion.Slerp(startRot, targetRot, lerpFactor);

                await UniTask.Yield(PlayerLoopTiming.Update, _cancellationToken.Token).SuppressCancellationThrow();
            }
        }

        private void UpdateChase()
        {
            float distance = Vector3.Distance(_view.transform.position, _model.LastKnownPosition);
            bool canSee = CheckVision(out Vector3 targetPos);

            if (canSee)
            {
                _model.SetTargetPosition(targetPos);
                _chaseTimer = _config.MemoryTime;

                if (distance <= _config.AttackRange)
                {
                    _model.SetState(AIState.Attack);
                    _view.StopMove();
                }
                else
                {
                    _view.MoveTo(_model.LastKnownPosition);
                }
            }
            else
            {
                _view.MoveTo(_model.LastKnownPosition);

                //Fixme: MagicNumbers
                if (distance < 2f)
                {
                    _chaseTimer -= Time.deltaTime;

                    if (_chaseTimer <= 0)
                    {
                        _model.SetState(AIState.Patrol);
                        _view.MoveTo(_view.PatrolPoints[_patrolIndex].position);
                    }
                }
            }
        }

        private void UpdateAttack()
        {
            if (_model.CurrentAmmo <= 0)
            {
                ReloadRoutine().Forget();
                return;
            }

            bool canSee = CheckVision(out Vector3 targetPos);
            float dist = Vector3.Distance(_view.transform.position, _model.LastKnownPosition);

            if (!canSee || dist > _config.AttackRange * 1.2f)
            {
                _model.SetState(AIState.Chase);
                return;
            }

            _model.SetTargetPosition(targetPos);
            _view.StopMove();
            _view.RotateTowards(_model.LastKnownPosition);

            if (Time.time >= _nextFireTime)
            {
                //Fixme: MagicNumbers
                _nextFireTime = Time.time + 1f / _config.FireRate;

                if (_model.TryConsumeAmmo())
                    _view.SpawnBullet(targetPos);
            }
        }

        private async UniTaskVoid ReloadRoutine()
        {
            _model.SetState(AIState.Reload);
            _isActionBusy = true;

            if (_view.FindCover(_model.LastKnownPosition, 15f, out Vector3 coverPos))
            {
                _view.MoveTo(coverPos);

                while (_view && _view.RemainingDistance > 1f)
                {
                    if (_cancellationToken.IsCancellationRequested)
                        return;

                    await UniTask.Yield(PlayerLoopTiming.Update, _cancellationToken.Token).SuppressCancellationThrow();
                }
            }

            if (_view == null || _cancellationToken.IsCancellationRequested)
                return;

            _view.StopMove();

            bool canceled = await UniTask.Delay(TimeSpan.FromSeconds(_config.ReloadTime), cancellationToken: _cancellationToken.Token)
                .SuppressCancellationThrow();

            if (canceled)
                return;

            if (!_view)
                return;

            _model.Reload(_config.MaxAmmo);
            _model.SetState(AIState.Chase);
            _isActionBusy = false;
        }

        private bool CheckVision(out Vector3 targetPos)
            => _view.CheckLineOfSight(_playerTransform, _config.SightDistance, _config.ViewAngle, _config.ViewMask, out targetPos);

        private bool TrySpotPlayer()
        {
            if (!_playerTransform)
                return false;

            if (CheckVision(out Vector3 pos))
            {
                _model.SetTargetPosition(pos);
                _model.SetState(AIState.Chase);
                return true;
            }

            return false;
        }

        private void OnTakeDamage(Vector3? hitPos, Vector3? force)
        {
            if (_playerTransform)
            {
                _model.SetTargetPosition(_playerTransform.position);
                _model.SetState(AIState.Chase);
            }
        }

        private void OnDeath() => _view.Die(null, null);
    }
}
</file>

<file path="Assets/_Main/Scripts/EnemyModule/Core/EnemyModel.cs">
using System;
using UnityEngine;

namespace EnemyModule
{
    public class EnemyModel
    {
        private AIState _currentState;
        private int _currentAmmo;
        private Vector3 _lastKnownPosition;
        private bool _hasTarget;

        public event Action<AIState> StateChanged;

        public AIState CurrentState => _currentState;
        public int CurrentAmmo => _currentAmmo;
        public Vector3 LastKnownPosition => _lastKnownPosition;
        public bool HasTarget => _hasTarget;

        public void Initialize(int maxAmmo, Vector3 startPos)
        {
            _currentAmmo = maxAmmo;
            _lastKnownPosition = startPos;
            SetState(AIState.Patrol);
        }

        public void SetState(AIState newState)
        {
            if (_currentState == newState)
                return;

            _currentState = newState;
            StateChanged?.Invoke(newState);
        }

        public void SetTargetPosition(Vector3 pos)
        {
            _lastKnownPosition = pos;
            _hasTarget = true;
        }

        //FIXME: Unused method
        public void ClearTarget() => _hasTarget = false;

        public bool TryConsumeAmmo()
        {
            if (_currentAmmo <= 0)
                return false;

            _currentAmmo--;
            return true;
        }

        public void Reload(int maxAmmo) => _currentAmmo = maxAmmo;
    }
}
</file>

<file path="Assets/_Main/Scripts/EnemyModule/Data/AIState.cs">
namespace EnemyModule
{
    public enum AIState
    {
        Patrol = 0,
        Chase,
        Attack,
        Reload,
    }
}
</file>

<file path="Assets/_Main/Scripts/EnemyModule/EnemyModule.asmdef">
{
    "name": "EnemyModule",
    "rootNamespace": "",
    "references": [
        "GUID:61af276ff4b779247aca1e9043c620bc",
        "GUID:0d8beb7f090555447a6cf5ce9e54dbb4",
        "GUID:f51ebe6a0ceec4240a699833d6309b23",
        "GUID:c962557d490c485478b5ea03480dc5b3",
        "GUID:ad92713fd5884a16a1e7ccde1a579228",
        "GUID:4c830bbd515e4f61bf5d288ad29d17db"
    ],
    "includePlatforms": [],
    "excludePlatforms": [],
    "allowUnsafeCode": false,
    "overrideReferences": false,
    "precompiledReferences": [],
    "autoReferenced": true,
    "defineConstraints": [],
    "versionDefines": [],
    "noEngineReferences": false
}
</file>

<file path="Assets/_Main/Scripts/EnemyModule/Installers/EnemyInstaller.cs">
using UnityEngine;
using Zenject;
using ComponentsModule;

namespace EnemyModule
{
    public class EnemyInstaller : MonoInstaller
    {
        [SerializeField] private EnemyConfig config;
        [SerializeField] private EnemyView view;

        public override void InstallBindings()
        {
            Container.BindInstance(config).AsSingle();
            Container.BindInstance(view).AsSingle();

            Container.Bind(typeof(IHealthComponent), typeof(IDamageable))
                .To<HealthComponent>()
                .AsSingle()
                .WithArguments(config.MaxHealth);

            Container.Bind<EnemyModel>().AsSingle();

            Container.BindInterfacesTo<EnemyController>()
                .AsSingle()
                .NonLazy();
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/EnemyModule/View/EnemyRagdoll.cs">
using UnityEngine;
using UnityEngine.AI;

namespace EnemyModule
{
    public class EnemyRagdoll : MonoBehaviour
    {
        [SerializeField] private Animator animator;
        [SerializeField] private NavMeshAgent agent;
        [SerializeField] private Collider mainCollider;

        private Rigidbody[] _bodies;
        private Collider[] _colliders;

        private void Awake()
        {
            _bodies = GetComponentsInChildren<Rigidbody>();
            _colliders = GetComponentsInChildren<Collider>();
            ToggleRagdoll(false);
        }

        public void Activate(Vector3? force = null, Vector3? hitPoint = null)
        {
            if (animator)
                animator.enabled = false;

            if (agent)
                agent.enabled = false;

            if (mainCollider)
                mainCollider.enabled = false;

            ToggleRagdoll(true);

            if (force.HasValue && hitPoint.HasValue)
                ApplyForce(hitPoint.Value, force.Value);
        }

        private void ToggleRagdoll(bool state)
        {
            foreach (var body in _bodies)
                body.isKinematic = !state;

            foreach (var collider in _colliders)
            {
                if (collider != mainCollider)
                {
                    collider.enabled = state;
                }
            }
        }

        private void ApplyForce(Vector3 position, Vector3 direction)
        {
            Rigidbody closest = null;
            float minDistance = float.MaxValue;

            foreach (var body in _bodies)
            {
                float distance = Vector3.Distance(body.position, position);

                if (distance < minDistance)
                {
                    minDistance = distance;
                    closest = body;
                }
            }

            if (closest)
                closest.AddForce(direction, ForceMode.Impulse);
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/EnemyModule/View/EnemyView.cs">
using ComponentsModule;
using UnityEngine;
using UnityEngine.AI;
using WeaponModule;
using Zenject;

namespace EnemyModule
{
    public class EnemyView : MonoBehaviour
    {
        [Header("References")] [SerializeField] private Transform eyes;
        [SerializeField] private Transform firePoint;
        [SerializeField] private GameObject bulletPrefab;
        [SerializeField] private Animator animator;
        [SerializeField] private EnemyRagdoll ragdoll;
        [SerializeField] private Transform[] patrolPoints;

        [SerializeField] private NavMeshAgent agent;

        private IHealthComponent _health;
        private EnemyConfig _config;

        public Transform[] PatrolPoints => patrolPoints;
        public IHealthComponent Health => _health;
        public Transform Eyes => eyes;
        public Transform FirePoint => firePoint;

        public bool IsPathPending => agent.pathPending;
        public float RemainingDistance => agent.remainingDistance;
        public Vector3 Velocity => agent != null ? agent.velocity : Vector3.zero;

        [Inject]
        public void Construct(IHealthComponent health)
        {
            _health = health;
        }

        public void Initialize(EnemyConfig config)
        {
            _config = config;
            agent.stoppingDistance = 0.5f;
        }

        public void MoveTo(Vector3 position)
        {
            if (agent.enabled)
            {
                agent.isStopped = false;
                agent.SetDestination(position);
            }
        }

        public void StopMove()
        {
            if (agent.enabled)
            {
                agent.isStopped = true;
                agent.velocity = Vector3.zero;
            }
        }

        //FIXME: Unused method
        public void ResetPath()
        {
            if (agent.enabled)
                agent.ResetPath();
        }

        public void RotateTowards(Vector3 target)
        {
            Vector3 direction = (target - transform.position).normalized;
            direction.y = 0;

            if (direction == Vector3.zero)
                return;

            var targetRotation = Quaternion.LookRotation(direction);

            //FIXME: Magic numbers
            transform.rotation = Quaternion.Slerp(transform.rotation, targetRotation, Time.deltaTime * 10f);
        }

        public void UpdateAnimator(Vector3 velocity)
        {
            if (animator)
            {
                bool isMoving = velocity.sqrMagnitude > 0.01f;
                //TODO: Вынести в класс с константами для аниматора и сделать через хэш
                animator.SetBool("IsMoving", isMoving);
            }
        }

        public void SpawnBullet(Vector3 targetPos)
        {
            //TODO: Вынести в класс с константами для аниматора и сделать через хэш
            if (animator)
                animator.SetTrigger("Shoot");

            Vector3 dir = (targetPos - firePoint.position).normalized;
            GameObject bulletObj = Instantiate(bulletPrefab, firePoint.position, Quaternion.LookRotation(dir));

            if (bulletObj.TryGetComponent(out Bullet bullet))
                bullet.Setup(_config.Damage, _config.BulletSpeed, 0f, Vector3.zero);
        }

        public void Die(Vector3? hitPos, Vector3? hitForce)
        {
            if (ragdoll)
                ragdoll.Activate(hitForce, hitPos);
            else
                Destroy(gameObject);

            //FIXME: Magic numbers
            if (ragdoll)
                Destroy(gameObject, 10f);
        }

        public bool CheckLineOfSight(Transform target, float range, float angle, LayerMask mask, out Vector3 hitPoint)
        {
            hitPoint = target.position;

            if (target == null)
                return false;

            Vector3 targetCenter;

            //FIXME: Magic numbers
            if (target.TryGetComponent(out Collider targetCol))
                targetCenter = targetCol.bounds.center;
            else
                targetCenter = target.position + Vector3.up * 1.0f;

            Debug.DrawLine(eyes.position, targetCenter, Color.yellow);
            float dist = Vector3.Distance(eyes.position, targetCenter);
            Vector3 dir = (targetCenter - eyes.position).normalized;
            float angleToTarget = Vector3.Angle(eyes.forward, dir);

            if (dist > range)
                return false;

            //FIXME: Magic numbers
            if (angleToTarget > angle / 2)
                return false;

            if (Physics.Raycast(eyes.position, dir, out RaycastHit hit, range, mask))
            {
                if (hit.transform.root == target.root)
                {
                    hitPoint = targetCenter;
                    return true;
                }
            }

            return false;
        }

        public bool FindCover(Vector3 threatPos, float radius, out Vector3 coverPos)
        {
            //FIXME: Magic numbers
            //Fixme: Разбить, максимальная вложенность 2
            for (int i = 0; i < 10; i++)
            {
                Vector3 randomPoint = transform.position + Random.insideUnitSphere * radius;

                if (NavMesh.SamplePosition(randomPoint, out NavMeshHit hit, 2f, NavMesh.AllAreas))
                {
                    if (Physics.Raycast(threatPos + Vector3.up, (hit.position - threatPos).normalized, out RaycastHit rayHit, Vector3.Distance(threatPos, hit.position), _config.ViewMask))
                    {
                        if (rayHit.transform.root != transform.root)
                        {
                            coverPos = hit.position;
                            return true;
                        }
                    }
                }
            }

            coverPos = transform.position;
            return false;
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/ExtractionModule/Configs/ExtractionConfig.cs">
using UnityEngine;

namespace ExtractionModule
{
    [CreateAssetMenu(fileName = "ExtractionConfig", menuName = "Configs/Extraction")]
    //FIXME: Tooltip на английском
    public class ExtractionConfig : ScriptableObject
    {
        [Tooltip("Время в секундах, которое нужно простоять в зоне")]
        public float ExtractionTime = 5.0f;

        [Tooltip("Текст таймера. {0} заменится на время.")]
        public string TimerTextFormat = "EVAC IN: {0:F1}";
    }
}
</file>

<file path="Assets/_Main/Scripts/ExtractionModule/Controllers/ExtractionController.cs">
using System;
using System.Threading;
using Cysharp.Threading.Tasks;
using Zenject;
using UnityEngine;
using InputModule;

namespace ExtractionModule
{
    public class ExtractionController : IInitializable, IDisposable
    {
        private readonly ExtractionConfig _config;
        private readonly ExtractionZoneView _zoneView;
        private readonly ExtractionHUDView _hudView;
        private readonly IInputService _input;

        private CancellationTokenSource _tokenSource;

        public ExtractionController(ExtractionConfig config,
            ExtractionZoneView zoneView,
            ExtractionHUDView hudView,
            IInputService input)
        {
            _config = config;
            _zoneView = zoneView;
            _hudView = hudView;
            _input = input;
        }

        public void Initialize()
        {
            if (_zoneView != null)
                _zoneView.PlayerPresenceChanged += OnZoneStateChanged;
        }

        public void Dispose()
        {
            if (_zoneView != null)
                _zoneView.PlayerPresenceChanged -= OnZoneStateChanged;

            CancelExtraction();
        }

        private void OnZoneStateChanged(bool isInside)
        {
            if (isInside)
                StartExtraction().Forget();
            else
                CancelExtraction();
        }

        private void CancelExtraction()
        {
            if (_tokenSource != null)
            {
                _tokenSource.Cancel();
                _tokenSource.Dispose();
                _tokenSource = null;
            }

            if (_hudView && !_hudView.Equals(null))
                _hudView.HideTimer();
        }

        private async UniTaskVoid StartExtraction()
        {
            CancelExtraction();
            _tokenSource = new();

            var token = _tokenSource.Token;
            var timer = _config.ExtractionTime;

            while (timer > 0)
            {
                if (_hudView == null || _hudView.Equals(null))
                    return;

                _hudView.ShowTimer(timer, _config.TimerTextFormat);
                bool isCanceled = await UniTask.Yield(PlayerLoopTiming.Update, token).SuppressCancellationThrow();

                if (isCanceled)
                    return;

                timer -= Time.deltaTime;
            }

            CompleteExtraction();
        }

        private void CompleteExtraction()
        {
            if (_tokenSource != null)
            {
                _tokenSource.Dispose();
                _tokenSource = null;
            }

            if (_hudView != null)
            {
                _hudView.HideTimer();
                _hudView.ShowSuccessScreen();
            }

            _input?.Disable();
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/ExtractionModule/ExtractionModule.asmdef">
{
    "name": "ExtractionModule",
    "rootNamespace": "",
    "references": [
        "GUID:0d8beb7f090555447a6cf5ce9e54dbb4",
        "GUID:f51ebe6a0ceec4240a699833d6309b23",
        "GUID:5a8d3845af48dc54db440d2b68ad43e7",
        "GUID:6055be8ebefd69e48b49212b09b47b2f"
    ],
    "includePlatforms": [],
    "excludePlatforms": [],
    "allowUnsafeCode": false,
    "overrideReferences": false,
    "precompiledReferences": [],
    "autoReferenced": true,
    "defineConstraints": [],
    "versionDefines": [],
    "noEngineReferences": false
}
</file>

<file path="Assets/_Main/Scripts/ExtractionModule/Installers/ExtractionInstaller.cs">
using UnityEngine;
using Zenject;

namespace ExtractionModule
{
    public class ExtractionInstaller : MonoInstaller
    {
        [SerializeField] private ExtractionConfig config;
        [SerializeField] private ExtractionZoneView zoneView;
        [SerializeField] private ExtractionHUDView hudView;

        public override void InstallBindings()
        {
            Container.BindInstance(config).AsSingle();
            Container.BindInstance(zoneView).AsSingle();
            Container.BindInstance(hudView).AsSingle();

            Container.BindInterfacesAndSelfTo<ExtractionController>().AsSingle();
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/ExtractionModule/View/ExtractionHUDView.cs">
using UnityEngine;
using TMPro;
using UnityEngine.UI;
using UnityEngine.SceneManagement;

namespace ExtractionModule
{
    public class ExtractionHUDView : MonoBehaviour
    {
        [Header("Timer UI")]
        [SerializeField] private GameObject timerContainer;
        [SerializeField] private TMP_Text timerText;

        [Header("Success UI")]
        [SerializeField] private GameObject successPanel;
        [SerializeField] private Button restartButton;

        private void Awake()
        {
            timerContainer?.SetActive(false);
            successPanel?.SetActive(false);

            if (restartButton)
                restartButton.onClick.AddListener(OnRestartClicked);
        }

        public void ShowTimer(float timeRemaining, string textFormat)
        {
            timerContainer?.SetActive(true);
            timerText.text = string.Format(textFormat, timeRemaining);
        }

        public void HideTimer() => timerContainer?.SetActive(false);

        public void ShowSuccessScreen()
        {
            if (!successPanel)
                return;

            successPanel.SetActive(true);
            Cursor.lockState = CursorLockMode.None;
            Cursor.visible = true;
        }

        private void OnRestartClicked() => SceneManager.LoadScene(SceneManager.GetActiveScene().name);
    }
}
</file>

<file path="Assets/_Main/Scripts/ExtractionModule/View/ExtractionZoneView.cs">
using System;
using UnityEngine;

namespace ExtractionModule
{
    public class ExtractionZoneView : MonoBehaviour
    {
        public event Action<bool> PlayerPresenceChanged;

        private void OnTriggerEnter(Collider other)
        {
            if (other.CompareTag("Player"))
                PlayerPresenceChanged?.Invoke(true);
        }

        private void OnTriggerExit(Collider other)
        {
            if (other.CompareTag("Player"))
                PlayerPresenceChanged?.Invoke(false);
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/GameCycleModule/GameCycleModule.asmdef">
{
    "name": "GameCycleModule",
    "rootNamespace": "",
    "references": [
        "GUID:0d8beb7f090555447a6cf5ce9e54dbb4",
        "GUID:4c830bbd515e4f61bf5d288ad29d17db",
        "GUID:ad92713fd5884a16a1e7ccde1a579228",
        "GUID:5a8d3845af48dc54db440d2b68ad43e7"
    ],
    "includePlatforms": [],
    "excludePlatforms": [],
    "allowUnsafeCode": false,
    "overrideReferences": false,
    "precompiledReferences": [],
    "autoReferenced": true,
    "defineConstraints": [],
    "versionDefines": [],
    "noEngineReferences": false
}
</file>

<file path="Assets/_Main/Scripts/GameCycleModule/Runtime/Core/GameSessionService.cs">
using System;
using UnityEngine.SceneManagement;
using UnityEngine;
using Zenject;

namespace GameCycleModule
{
    public class GameSessionService : IGameSessionService, IInitializable, IDisposable
    {
        public void Initialize()
        {
            Cursor.lockState = CursorLockMode.Locked;
            Cursor.visible = false;
        }

        public void Dispose()
        {
            Cursor.lockState = CursorLockMode.None;
            Cursor.visible = true;
        }

        public void RestartLevel()
        {
            var currentScene = SceneManager.GetActiveScene().name;
            SceneManager.LoadScene(currentScene);
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/GameCycleModule/Runtime/Core/IGameSessionService.cs">
namespace GameCycleModule
{
    public interface IGameSessionService
    {
        void RestartLevel();
    }
}
</file>

<file path="Assets/_Main/Scripts/GameCycleModule/Runtime/Installers/GameCycleInstaller.cs">
using PlayerModule;
using UnityEngine;
using Zenject;

namespace GameCycleModule
{
    public class GameCycleInstaller : MonoInstaller
    {
        [SerializeField] private GameOverView gameOverView;
        [SerializeField] private PlayerProvider playerProvider;

        public override void InstallBindings()
        {
            Container.Bind<PlayerProvider>()
                .FromInstance(playerProvider)
                .AsSingle();

            Container.BindInterfacesTo<GameSessionService>()
                .AsSingle()
                .NonLazy();

            Container.BindInterfacesTo<GameOverPresenter>()
                .AsSingle()
                .NonLazy();

            Container.Bind<GameOverView>()
                .FromInstance(gameOverView)
                .AsSingle();
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/GameCycleModule/Runtime/Presenters/GameOverPresenter.cs">
using ComponentsModule;
using System;
using InputModule;
using PlayerModule;
using Zenject;

namespace GameCycleModule
{
    public class GameOverPresenter : IInitializable, IDisposable
    {
        private readonly PlayerProvider _player;
        private readonly GameOverView _view;

        private readonly IGameSessionService _sessionService;
        private readonly IInputService _inputService;

        public GameOverPresenter(PlayerProvider player,
            GameOverView view,
            IGameSessionService sessionService,
            IInputService inputService)
        {
            _player = player;
            _view = view;

            _sessionService = sessionService;
            _inputService = inputService;
        }

        public void Initialize()
        {
            _view.Hide();
            _player.Get<IHealthComponent>().Died += OnDied;
            _view.RestartClicked += OnRestartClicked;
        }

        public void Dispose()
        {
            _player.Get<IHealthComponent>().Died -= OnDied;
            _view.RestartClicked -= OnRestartClicked;
        }

        private void OnDied()
        {
            _inputService.Disable();
            _view.Show();
        }

        private void OnRestartClicked()
        {
            _sessionService.RestartLevel();
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/GameCycleModule/Runtime/View/GameOverView.cs">
using UnityEngine;
using UnityEngine.UI;
using System;

namespace GameCycleModule
{
    public class GameOverView : MonoBehaviour
    {
        [SerializeField] private GameObject panel;
        [SerializeField] private Button restartButton;

        public event Action RestartClicked;

        private void OnEnable() => restartButton.onClick.AddListener(OnRestartButtonClicked);

        private void OnDisable() => restartButton.onClick.RemoveListener(OnRestartButtonClicked);

        public void Show()
        {
            panel.SetActive(true);
            Cursor.lockState = CursorLockMode.None;
            Cursor.visible = true;
        }

        public void Hide() => panel.SetActive(false);

        private void OnRestartButtonClicked() => RestartClicked?.Invoke();
    }
}
</file>

<file path="Assets/_Main/Scripts/InputModule/InputModule.asmdef">
{
    "name": "InputModule",
    "rootNamespace": "",
    "references": [
        "GUID:75469ad4d38634e559750d17036d5f7c",
        "GUID:0d8beb7f090555447a6cf5ce9e54dbb4"
    ],
    "includePlatforms": [],
    "excludePlatforms": [],
    "allowUnsafeCode": false,
    "overrideReferences": false,
    "precompiledReferences": [],
    "autoReferenced": true,
    "defineConstraints": [],
    "versionDefines": [],
    "noEngineReferences": false
}
</file>

<file path="Assets/_Main/Scripts/InputModule/Installers/InputInstaller.cs">
using UnityEngine;
using Zenject;

namespace InputModule
{
    [CreateAssetMenu(fileName = "InputInstaller", menuName = "Installers/InputInstaller")]
    public class InputInstaller : ScriptableObjectInstaller
    {
        public override void InstallBindings()
        {
            Container.BindInterfacesTo<GameInputService>().AsSingle();
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/InventoryModule/InventoryModule.asmdef">
{
    "name": "InventoryModule",
    "rootNamespace": "",
    "references": [
        "GUID:0d8beb7f090555447a6cf5ce9e54dbb4",
        "GUID:5a8d3845af48dc54db440d2b68ad43e7",
        "GUID:6055be8ebefd69e48b49212b09b47b2f",
        "GUID:f51ebe6a0ceec4240a699833d6309b23"
    ],
    "includePlatforms": [],
    "excludePlatforms": [],
    "allowUnsafeCode": false,
    "overrideReferences": false,
    "precompiledReferences": [],
    "autoReferenced": true,
    "defineConstraints": [],
    "versionDefines": [],
    "noEngineReferences": false
}
</file>

<file path="Assets/_Main/Scripts/InventoryModule/Runtime/Core/GridCell.cs">
using UnityEngine;

namespace InventoryModule
{
    public class GridCell
    {
        public Vector2Int Position { get; }
        public InventoryItem OccupyingItem { get; private set; }
        public bool IsOccupied => OccupyingItem != null;

        public GridCell(Vector2Int position)
        {
            Position = position;
        }

        public void Occupy(InventoryItem item) => OccupyingItem = item;
        public void Vacate() => OccupyingItem = null;
    }
}
</file>

<file path="Assets/_Main/Scripts/InventoryModule/Runtime/Data/InventoryConfig.cs">
using UnityEngine;

namespace InventoryModule
{
    [CreateAssetMenu(fileName = "InventoryConfig", menuName = "Configs/Inventory/InventoryConfig")]
    public class InventoryConfig : ScriptableObject
    {
        [SerializeField] private int width = 10;
        [SerializeField] private int height = 5;

        public int Width => width;
        public int Height => height;
    }
}
</file>

<file path="Assets/_Main/Scripts/InventoryModule/Runtime/View/LootItemView.cs">
using UnityEngine;

namespace InventoryModule
{
    public class LootItemView : MonoBehaviour
    {
        [SerializeField] private ItemDefinition itemDefinition;
        [SerializeField] private int amount = 1;

        [SerializeField] private MeshRenderer meshRenderer;
        [SerializeField] private ParticleSystem pickupParticles;
        [SerializeField] private AudioClip pickupSound;

        private ItemDatabase _database;
        private ItemDefinition _cachedDefinition;

        public ItemDefinition ItemDef => GetItemDefinition();
        public int Amount => amount;

        public void SetAmount(int newAmount)
        {
            amount = Mathf.Max(1, newAmount);
        }

        private ItemDefinition GetItemDefinition()
        {
            if (_cachedDefinition != null)
                return _cachedDefinition;

            if (itemDefinition != null)
            {
                _cachedDefinition = itemDefinition;
                return _cachedDefinition;
            }

            return null;
        }

        private void OnValidate()
        {
            if (itemDefinition != null && itemDefinition.WorldPrefab != null)
            {
            }
        }

        private void UpdateVisual()
        {
        }

        public void Collect()
        {
            if (pickupParticles != null)
            {
                var particles = Instantiate(pickupParticles, transform.position, Quaternion.identity);
                Destroy(particles.gameObject, 2f);
            }

            if (pickupSound != null)
            {
                AudioSource.PlayClipAtPoint(pickupSound, transform.position);
            }

            Destroy(gameObject);
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/NpcBaseModule/NpcBaseModule.asmdef">
{
  "name": "NpcBaseModule",
  "references": [
    "Zenject",
    "Unity.TextMeshPro"
  ],
  "includePlatforms": [],
  "excludePlatforms": [],
  "allowUnsafeCode": false,
  "overrideReferences": false,
  "precompiledReferences": [],
  "autoReferenced": true,
  "defineConstraints": [],
  "versionDefines": [],
  "noEngineReferences": false
}
</file>

<file path="Assets/_Main/Scripts/NpcBaseModule/Runtime/Controllers/FriendlyNpcController.cs">
using UnityEngine;
using UnityEngine.AI;
using Zenject;

namespace NpcBaseModule
{
    public sealed class FriendlyNpcController : ITickable, IInitializable
    {
        private const float MinRemainingDistance = 0.1f;
        private const float RetryWaitSeconds = 0.5f;
        private const int NavMeshSampleAttempts = 30;
        private const float MinDirectionSqrMagnitude = 0.0001f;

        private readonly FriendlyNpcView _view;

        private FriendlyNpcState _state;
        private Transform _player;

        private float _waitTimer;
        private Vector3 _currentDestination;
        private bool _hasDestination;

        public FriendlyNpcController(FriendlyNpcView view)
        {
            _view = view;
            _state = FriendlyNpcState.Patrolling;
        }

        public void Initialize()
        {
            _view.SetController(this);

            ApplyAgentSettings();
            HideText();
            StartPatrol(forceNewPoint: true);
        }

        public void OnInteract(Transform player)
        {
            if (player == null)
                return;

            if (_state == FriendlyNpcState.Responding)
                return;

            _player = player;
            EnterResponding();
        }

        public void Tick()
        {
            if (_view.Agent == null)
                return;

            switch (_state)
            {
                case FriendlyNpcState.Patrolling:
                    TickPatrolling();
                    return;

                case FriendlyNpcState.Responding:
                    TickResponding();
                    return;

                default:
                    return;
            }
        }

        private void TickPatrolling()
        {
            NavMeshAgent agent = _view.Agent;

            if (!_hasDestination)
            {
                if (_waitTimer > 0f)
                {
                    _waitTimer -= Time.deltaTime;
                    return;
                }

                TrySetNewDestination();
                return;
            }

            if (agent.pathPending)
                return;

            float remainingDistanceThreshold = Mathf.Max(agent.stoppingDistance, MinRemainingDistance);

            if (agent.remainingDistance <= remainingDistanceThreshold)
            {
                _hasDestination = false;
                _waitTimer = Random.Range(_view.Settings.MinWait, _view.Settings.MaxWait);
            }
        }

        private void TickResponding()
        {
            if (_player == null)
            {
                ExitResponding();
                return;
            }

            float distance = Vector3.Distance(_player.position, _view.Transform.position);

            if (distance > _view.Settings.ExitRadius)
            {
                ExitResponding();
                return;
            }

            FaceTarget(_player.position);
        }

        private void EnterResponding()
        {
            _state = FriendlyNpcState.Responding;

            NavMeshAgent agent = _view.Agent;
            agent.isStopped = true;
            agent.ResetPath();

            ShowText();
        }

        private void ExitResponding()
        {
            _player = null;
            HideText();

            NavMeshAgent agent = _view.Agent;
            agent.isStopped = false;

            _state = FriendlyNpcState.Patrolling;
            StartPatrol(forceNewPoint: true);
        }

        private void StartPatrol(bool forceNewPoint)
        {
            _hasDestination = false;
            _waitTimer = 0f;

            if (forceNewPoint)
                TrySetNewDestination();
        }

        private void TrySetNewDestination()
        {
            NavMeshAgent agent = _view.Agent;

            if (TryGetRandomNavMeshPoint(_view.Transform.position, _view.Settings.PatrolRadius, out Vector3 point))
            {
                _currentDestination = point;
                _hasDestination = true;
                agent.SetDestination(_currentDestination);
                return;
            }

            _hasDestination = false;
            _waitTimer = RetryWaitSeconds;
        }

        private bool TryGetRandomNavMeshPoint(Vector3 origin, float radius, out Vector3 result)
        {
            for (int attemptIndex = 0; attemptIndex < NavMeshSampleAttempts; attemptIndex++)
            {
                Vector3 randomPoint = origin + Random.insideUnitSphere * radius;

                if (NavMesh.SamplePosition(randomPoint, out NavMeshHit hit, radius, NavMesh.AllAreas))
                {
                    result = hit.position;
                    return true;
                }
            }

            result = origin;
            return false;
        }

        private void FaceTarget(Vector3 targetPosition)
        {
            Vector3 selfPosition = _view.Transform.position;
            Vector3 direction = targetPosition - selfPosition;
            direction.y = 0f;

            if (direction.sqrMagnitude < MinDirectionSqrMagnitude)
                return;

            float targetYaw = Quaternion.LookRotation(direction, Vector3.up).eulerAngles.y;
            float currentYaw = _view.Transform.eulerAngles.y;

            float newYaw = Mathf.MoveTowardsAngle(currentYaw, targetYaw, _view.Settings.TurnSpeed * Time.deltaTime);
            _view.Transform.rotation = Quaternion.Euler(0f, newYaw, 0f);
        }

        private void ApplyAgentSettings()
        {
            NavMeshAgent agent = _view.Agent;

            if (agent == null)
                return;

            agent.speed = _view.Settings.MoveSpeed;
        }

        private void ShowText() => _view.WorldText?.Show(_view.Settings.MessageText);

        private void HideText() => _view.WorldText?.Hide();
    }
}
</file>

<file path="Assets/_Main/Scripts/NpcBaseModule/Runtime/Core/IInteractable.cs">
using UnityEngine;

namespace NpcBaseModule
{
    public interface IInteractable
    {
        Transform Transform { get; }
        float InteractionRadius { get; }

        bool CanInteract(Transform interactor);
        void Interact(Transform interactor);
    }
}
</file>

<file path="Assets/_Main/Scripts/NpcBaseModule/Runtime/Core/PlayerInteractor.cs">
using UnityEngine;

namespace NpcBaseModule
{
    public sealed class PlayerInteractor : MonoBehaviour
    {
        [SerializeField] private Camera playerCamera;
        [SerializeField] private float maxDistance = 4f;
        [SerializeField] private LayerMask interactMask = ~0;
        [SerializeField] private KeyCode interactKey = KeyCode.E;
        [SerializeField] private Transform interactorRoot;

        private void Awake()
        {
            playerCamera ??= Camera.main;
            interactorRoot ??= transform;
        }

        private void Update()
        {
            if (!Input.GetKeyDown(interactKey))
                return;

            if (playerCamera == null)
                return;

            var ray = new Ray(playerCamera.transform.position, playerCamera.transform.forward);

            if (!Physics.Raycast(ray, out var hit, maxDistance, ~0, QueryTriggerInteraction.Ignore))
                return;

            var interactable = hit.collider.GetComponentInParent<IInteractable>();

            if (interactable == null)
                return;

            bool canInteract = interactable.CanInteract(interactorRoot);

            if (!canInteract)
                return;

            interactable.Interact(interactorRoot);
        }

        //FIXME: Remove unused method
        private bool TryGetInteractable(out IInteractable interactable)
        {
            interactable = null;

            var ray = new Ray(playerCamera.transform.position, playerCamera.transform.forward);

            if (!Physics.Raycast(ray, out var hit, maxDistance, interactMask, QueryTriggerInteraction.Ignore))
                return false;

            interactable = hit.collider.GetComponentInParent<IInteractable>();

            return interactable != null;
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/NpcBaseModule/Runtime/Data/FriendlyNpcSettings.cs">
using System;
using UnityEngine;

[Serializable]
public sealed class FriendlyNpcSettings
{
    [Min(0.5f)] public float InteractionRadius = 2f;
    [Min(0.5f)] public float ExitRadius = 4f;
    [Min(0.5f)] public float PatrolRadius = 6f;

    [Min(0.1f)] public float MoveSpeed = 2f;
    [Min(30f)] public float TurnSpeed = 360f;

    [Min(0f)] public float MinWait = 0.5f;
    [Min(0f)] public float MaxWait = 2f;

    [TextArea] public string MessageText;
}
</file>

<file path="Assets/_Main/Scripts/NpcBaseModule/Runtime/Data/FriendlyNpcState.cs">
namespace NpcBaseModule
{
    public enum FriendlyNpcState
    {
        Patrolling,
        Responding,
    }
}
</file>

<file path="Assets/_Main/Scripts/NpcBaseModule/Runtime/Installers/FriendlyNpcInstaller.cs">
using UnityEngine;
using Zenject;

namespace NpcBaseModule
{
    public sealed class FriendlyNpcInstaller : MonoInstaller
    {
        [SerializeField] private FriendlyNpcView view;

        private void OnValidate() => view ??= GetComponent<FriendlyNpcView>();

        public override void InstallBindings()
        {
            Container.Bind<FriendlyNpcView>()
                .FromInstance(view)
                .AsSingle();

            Container.BindInterfacesAndSelfTo<FriendlyNpcController>()
                .AsSingle()
                .NonLazy();
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/NpcBaseModule/Runtime/View/FriendlyNpcView.cs">
using UnityEngine;
using UnityEngine.AI;

namespace NpcBaseModule
{
    public sealed class FriendlyNpcView : MonoBehaviour, IInteractable
    {
        [SerializeField] private FriendlyNpcSettings settings;
        [SerializeField] private NavMeshAgent agent;
        [SerializeField] private Transform headPoint;
        [SerializeField] private NpcWorldTextView worldText;

        private FriendlyNpcController _controller;

        public Transform Transform => transform;
        public float InteractionRadius => settings.InteractionRadius;

        public FriendlyNpcSettings Settings => settings;
        public NavMeshAgent Agent => agent;
        public Transform HeadPoint => headPoint;
        public NpcWorldTextView WorldText => worldText;

        private void Awake()
        {
            agent ??= GetComponent<NavMeshAgent>();
            worldText ??= GetComponentInChildren<NpcWorldTextView>(true);
        }

        public void SetController(FriendlyNpcController controller) => _controller = controller;

        public bool CanInteract(Transform interactor)
        {
            if (interactor == null)
                return false;

            Vector3 interactorPosition = interactor.position;
            Vector3 npcPosition = transform.position;

            interactorPosition.y = 0f;
            npcPosition.y = 0f;

            float distance = Vector3.Distance(interactorPosition, npcPosition);
            return distance <= settings.InteractionRadius;
        }

        public void Interact(Transform interactor) => _controller?.OnInteract(interactor);
    }
}
</file>

<file path="Assets/_Main/Scripts/NpcBaseModule/Runtime/View/NpcWorldTextView.cs">
using TMPro;
using UnityEngine;

namespace NpcBaseModule
{
    public sealed class NpcWorldTextView : MonoBehaviour
    {
        [SerializeField] private GameObject root;
        [SerializeField] private TMP_Text text;

        private void Awake()
        {
            if (root == null)
            {
                var canvas = GetComponentInParent<Canvas>(true);
                root = canvas != null ? canvas.gameObject : gameObject;
            }

            text ??= GetComponentInChildren<TMP_Text>(true);
        }

        private void Start() => Hide();

        public void Show(string message)
        {
            text?.SetText(message);
            root?.SetActive(true);
        }

        public void Hide() => root?.SetActive(false);
    }
}
</file>

<file path="Assets/_Main/Scripts/PlayerModule/Runtime/Controllers/InventoryDeathHandler.cs">
using System;
using ComponentsModule;
using InventoryModule;
using Zenject;

namespace PlayerModule
{
    public class InventoryDeathHandler : IInitializable, IDisposable
    {
        private readonly PlayerProvider _playerProvider;
        private readonly InventoryModel _inventory;

        public InventoryDeathHandler(PlayerProvider playerProvider, InventoryModel inventory)
        {
            _playerProvider = playerProvider;
            _inventory = inventory;
        }

        public void Initialize()
        {
            if (_playerProvider.TryGet<IHealthComponent>(out var health))
                health.Died += OnDied;
        }

        public void Dispose()
        {
            if (_playerProvider.TryGet<IHealthComponent>(out var health))
                health.Died -= OnDied;
        }

        private void OnDied() => _inventory.Clear();
    }
}
</file>

<file path="Assets/_Main/Scripts/PlayerModule/Runtime/Controllers/PlayerInteractionController.cs">
using Cysharp.Threading.Tasks;
using InputModule;
using InventoryModule;
using System;
using System.Threading;
using UnityEngine;
using Zenject;

namespace PlayerModule
{
    public sealed class PlayerInteractionController : IInitializable, ITickable, IDisposable
    {
        private readonly IInputMap _input;
        private readonly IInventoryService _inventoryService;
        private readonly IItemProvider _itemProvider;
        private readonly Camera _camera;
        private readonly CancellationTokenSource _cancellationTokenSource;

        public PlayerInteractionController(
            IInputMap input,
            IInventoryService inventoryService,
            IItemProvider itemProvider)
        {
            _input = input ?? throw new ArgumentNullException(nameof(input));
            _inventoryService = inventoryService ?? throw new ArgumentNullException(nameof(inventoryService));
            _itemProvider = itemProvider ?? throw new ArgumentNullException(nameof(itemProvider));
            _camera = Camera.main;
            _cancellationTokenSource = new CancellationTokenSource();
        }

        public void Initialize()
        {
            // Initialization if needed
        }

        public void Dispose()
        {
            _cancellationTokenSource.Cancel();
            _cancellationTokenSource.Dispose();
        }

        public void Tick()
        {
            if (!_input.IsInteractPressed)
                return;

            var ray = _camera.ViewportPointToRay(new Vector3(0.5f, 0.5f, 0f));

            if (Physics.Raycast(ray, out var hit, 3f))
            {
                var loot = hit.collider.GetComponentInParent<LootItemView>();
                if (loot != null && loot.ItemDef != null)
                {
                    TryPickUpLootAsync(loot).Forget();
                }
            }
        }

        private async UniTaskVoid TryPickUpLootAsync(LootItemView loot)
        {
            var token = _cancellationTokenSource.Token;

            try
            {
                var itemDefinition = _itemProvider.GetItemDefinition(loot.ItemDef.Id);
                if (itemDefinition == null)
                {
                    Debug.LogWarning($"Item definition not found for loot: {loot.ItemDef.Id}");
                    return;
                }

                var result = await _inventoryService.AddItemAsync(
                    itemDefinition,
                    loot.Amount,
                    token);

                if (result.Success)
                {
                    loot.Collect();
                }
                else
                {
                    Debug.LogWarning($"Failed to pick up loot: {result.ErrorMessage}");
                }
            }
            catch (OperationCanceledException)
            {
                // Operation was cancelled, ignore
            }
            catch (Exception exception)
            {
                Debug.LogError($"Error picking up loot: {exception}");
            }
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/PlayerModule/Runtime/Controllers/PlayerMovementController.cs">
using ComponentsModule;
using InputModule;
using Zenject;

namespace PlayerModule
{
    public class PlayerMovementController : ITickable
    {
        private readonly IMoveComponent _mover;
        private readonly IRotationComponent _rotation;
        private readonly ICrouchComponent _croucher;
        private readonly ILeanComponent _leaner;
        private readonly IInputMap _inputMap;
        private readonly PlayerConfig _config;

        public PlayerMovementController(IMoveComponent mover,
            IRotationComponent rotation,
            ICrouchComponent croucher,
            ILeanComponent leaner,
            IInputMap inputMap,
            PlayerConfig config)
        {
            _mover = mover;
            _inputMap = inputMap;
            _rotation = rotation;
            _leaner = leaner;
            _croucher = croucher;
            _config = config;
        }

        public void Tick()
        {
            Move();
            Rotate();
            Crouch();
            Lean();
        }

        private void Move()
        {
            var isCrouching = _inputMap.IsCrouchPressed;
            var jumped = _inputMap.IsJumpPressed && !isCrouching;

            var targetSpeed = isCrouching ? _config.CrouchSpeed : _config.WalkSpeed;
            _mover.SetSpeed(targetSpeed);

            var direction = _inputMap.MoveInput;
            _mover.Move(direction, jumped);
        }

        private void Rotate()
        {
            var direction = _inputMap.LookInput;
            _rotation.Rotate(direction);
        }

        private void Lean()
        {
            var targetLean = _inputMap.LeanInput;
            _leaner.Lean(targetLean);
        }

        private void Crouch()
        {
            var isCrouching = _inputMap.IsCrouchPressed;
            _croucher.Crouch(isCrouching);
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/PlayerModule/Runtime/Core/PlayerProvider.cs">
using UnityEngine;
using Zenject;

namespace PlayerModule
{
    public class PlayerProvider : MonoBehaviour
    {
        [SerializeField] private GameObjectContext context;

        private DiContainer _container;

        private void Awake()
        {
            _container = context.Container;
        }

        private void OnValidate()
        {
            context ??= GetComponent<GameObjectContext>();
        }

        public T Get<T>() where T : class => _container.Resolve<T>();

        public bool TryGet<T>(out T value) where T : class
        {
            value = _container.TryResolve<T>();
            return value != null;
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/PlayerModule/Runtime/Data/PlayerConfig.cs">
using ComponentsModule;
using UnityEngine;

namespace PlayerModule
{
    [CreateAssetMenu(fileName = "PlayerConfig", menuName = "Configs/PlayerConfig")]
    public class PlayerConfig : ScriptableObject
    {
        [Header("Health")]
        [SerializeField] private float maxHealth = 100f;
        
        [Header("Movement")]
        [SerializeField] private float walkSpeed = 5f;
        [SerializeField] private float crouchSpeed = 2.5f;
        [SerializeField] private float gravity = -15f;
        [SerializeField] private float jumpHeight = 1.2f;
        
        [Header("Look")]
        [SerializeField] private float mouseSensitivity = 0.1f;
        [SerializeField] private float lookXLimit = 85f;
        
        [Header("Lean")]
        [SerializeField] private float leanAngle = 15f;
        [SerializeField] private float leanOffset = 0.5f;
        [SerializeField] private float leanSpeed = 10f;
        
        [Header("Crouch")]
        [SerializeField] private CrouchData crouchData;
        
        public float MaxHealth => maxHealth;
        public float WalkSpeed => walkSpeed;
        public float CrouchSpeed => crouchSpeed;
        
        public float Gravity => gravity;
        public float JumpHeight => jumpHeight;
        
        public float MouseSensitivity => mouseSensitivity;
        public float LookXLimit => lookXLimit;

        public float LeanAngle => leanAngle;
        public float LeanOffset => leanOffset;
        public float LeanSpeed => leanSpeed;
        
        public CrouchData CrouchData => crouchData;
    }
}
</file>

<file path="Assets/_Main/Scripts/UIModule/Runtime/Presenters/HealthPresenter.cs">
using System;
using Zenject;
using ComponentsModule;

namespace UIModule
{
    public class HealthPresenter : IInitializable, IDisposable
    {
        private readonly IHealthComponent _health;
        private readonly HealthView _view;

        public HealthPresenter(IHealthComponent health, HealthView view)
        {
            _health = health;
            _view = view;
        }

        public void Initialize()
        {
            OnHealthChanged(_health.CurrentHealth, _health.MaxHealth);
            _health.HealthChanged += OnHealthChanged;
        }

        public void Dispose() => _health.HealthChanged -= OnHealthChanged;

        private void OnHealthChanged(float current, float max) => _view.SetHealth(current, max);
    }
}
</file>

<file path="Assets/_Main/Scripts/UIModule/Runtime/View/HealthView.cs">
using UnityEngine;
using UnityEngine.UI;
using TMPro;

namespace UIModule
{
    public class HealthView : MonoBehaviour
    {
        [SerializeField] private Slider healthSlider;
        [SerializeField] private TMP_Text healthText;

        private void Awake()
        {
            if (healthSlider != null)
                healthSlider.interactable = false;
        }

        public void SetHealth(float current, float max)
        {
            if (healthSlider)
            {
                healthSlider.maxValue = max;
                healthSlider.value = current;
            }

            if (healthText)
                healthText.text = $"{Mathf.CeilToInt(current)} / {max}";
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/UIModule/UIModule.asmdef">
{
    "name": "UIModule",
    "rootNamespace": "",
    "references": [
        "GUID:ad92713fd5884a16a1e7ccde1a579228",
        "GUID:5a8d3845af48dc54db440d2b68ad43e7",
        "GUID:6055be8ebefd69e48b49212b09b47b2f"
    ],
    "includePlatforms": [],
    "excludePlatforms": [],
    "allowUnsafeCode": false,
    "overrideReferences": false,
    "precompiledReferences": [],
    "autoReferenced": true,
    "defineConstraints": [],
    "versionDefines": [],
    "noEngineReferences": false
}
</file>

<file path="Assets/_Main/Scripts/WeaponModule/Content/Bullet.cs">
using ComponentsModule;
using UnityEngine;

namespace WeaponModule
{
    public class Bullet : MonoBehaviour
    {
        [Header("Settings")]
        [SerializeField] private float lifeTime = 3f;
        [SerializeField] private GameObject hitEffect;

        private float _damage;
        private Rigidbody _rigidbody;
        private TrailRenderer _trail;

        private void Awake()
        {
            //FIXME: Через SerializeField
            _rigidbody = GetComponent<Rigidbody>();
            _trail = GetComponent<TrailRenderer>();

            if (_trail != null)
                _trail.enabled = false;
        }

        public void Setup(float damage, float bulletSpeed, float inheritFactor, Vector3 shooterVelocity)
        {
            _damage = damage;

            if (_rigidbody != null)
            {
                _rigidbody.linearVelocity = Vector3.zero;
                _rigidbody.angularVelocity = Vector3.zero;

                Vector3 bulletVel = transform.forward * bulletSpeed;
                Vector3 playerVel = shooterVelocity * inheritFactor;
                _rigidbody.linearVelocity = bulletVel + playerVel;
            }

            if (_trail != null)
            {
                _trail.Clear();
                _trail.enabled = true;
            }

            Destroy(gameObject, lifeTime);
        }

        private void OnCollisionEnter(Collision collision)
        {
            //TODO: Через прокси
            var target = collision.gameObject.GetComponentInParent<IDamageable>();

            if (target != null)
            {
                Vector3 force = transform.forward * 10f;
                target.TakeDamage(_damage, collision.contacts[0].point, force);
            }

            if (hitEffect != null)
            {
                ContactPoint contact = collision.contacts[0];
                //FIXME: Magic numbers
                GameObject effect = Instantiate(hitEffect, contact.point + contact.normal * 0.05f, Quaternion.LookRotation(contact.normal));
                Destroy(effect, 2f);
            }

            Destroy(gameObject);
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/WeaponModule/Controllers/WeaponController.cs">
using System;
using System.Threading;
using Cysharp.Threading.Tasks;
using InputModule;
using UnityEngine;
using Zenject;

namespace WeaponModule
{
    public class WeaponController : IWeapon, IInitializable, ITickable, ILateTickable, IDisposable
    {
        private readonly WeaponConfig _config;
        private readonly WeaponModel _model;
        private readonly WeaponView _view;
        private readonly IInputMap _input;

        private float _nextFireTime;
        private bool _isReloading;
        private bool _isAiming;
        private bool _isAimToggleActive;

        private bool _shouldShootFrame;

        private CancellationTokenSource _tokenSource;

        public WeaponController(WeaponConfig config,
            WeaponModel model,
            WeaponView view,
            IInputMap input)
        {
            _config = config;
            _model = model;
            _view = view;
            _input = input;

            _tokenSource = new();
        }

        public void Initialize()
        {
            _model.Initialize(_config.MaxAmmo);
            _view.Initialize(_config);
        }

        public void Equip()
        {
            _view.gameObject.SetActive(true);
            _isReloading = false;
            _isAiming = false;
            _isAimToggleActive = false;
            _shouldShootFrame = false;

            DrawWeaponRoutine().Forget();
        }

        public async UniTask Unequip()
        {
            CancelCurrentActions();
            _view.SetHolsterState(true);
            await UniTask.Delay(TimeSpan.FromSeconds(_config.DrawTime));

            _view.gameObject.SetActive(false);
        }

        public void Dispose()
        {
            CancelCurrentActions();
            _tokenSource.Dispose();
        }

        public void Tick()
        {
            if (!_view.gameObject.activeSelf)
                return;

            HandleAimingState();
            CheckFireInput();
            HandleReload();
            HandleTriggerFinger();
            HandleMovementAnim();
        }

        public void LateTick()
        {
            if (!_view.gameObject.activeSelf)
                return;

            HandleProceduralAnimation();

            if (_shouldShootFrame)
            {
                Fire();
                _shouldShootFrame = false;
            }
        }

        private void HandleProceduralAnimation()
        {
            Vector2 lookDelta = _input.LookInput;
            _view.UpdateProcedural(Time.deltaTime, lookDelta, _isAiming);
        }

        private void HandleAimingState()
        {
            if (_isReloading)
            {
                _isAiming = false;
                _isAimToggleActive = false;
            }
            else
            {
                if (_config.AimIsToggle)
                {
                    if (_input.IsAimTriggered)
                        _isAimToggleActive = !_isAimToggleActive;

                    _isAiming = _isAimToggleActive;
                }
                else
                {
                    _isAiming = _input.IsAimPressed;
                }
            }

            _view.UpdateAiming(_isAiming, _config.AimStability);
        }

        private void CheckFireInput()
        {
            if (_isReloading)
                return;

            if (_input.IsFirePressed && Time.time >= _nextFireTime)
            {
                if (_model.CurrentAmmo > 0)
                {
                    _shouldShootFrame = true;
                    _nextFireTime = Time.time + _config.FireRate;
                }
            }
        }

        private void Fire()
        {
            if (_model.TryConsumeAmmo())
            {
                _view.PlayFireEffects(_isAiming);
                _view.SpawnBullet(_config.Damage, _config.BulletSpeed);
            }
        }

        private void HandleReload()
        {
            if (_isReloading)
                return;

            if (_input.IsReloadPressed && !_model.IsFull)
                ReloadRoutine().Forget();
        }

        private void HandleMovementAnim()
        {
            Vector2 input = _input.MoveInput;
            bool isMoving = input.magnitude > 0.1f;
            _view.SetMovementState(isMoving, input);
        }

        private void HandleTriggerFinger()
        {
            _view.SetTriggerHold(_input.IsFirePressed);
        }

        private async UniTaskVoid ReloadRoutine()
        {
            _isReloading = true;
            _view.PlayReload();

            bool canceled = await UniTask.Delay(TimeSpan.FromSeconds(_config.ReloadTime), cancellationToken: _tokenSource.Token)
                .SuppressCancellationThrow();

            if (!canceled)
                _model.Reload();

            _isReloading = false;
        }

        private async UniTaskVoid DrawWeaponRoutine()
        {
            _view.SetHolsterState(false);
            _isReloading = true;

            bool canceled = await UniTask.Delay(TimeSpan.FromSeconds(_config.DrawTime), cancellationToken: _tokenSource.Token)
                .SuppressCancellationThrow();

            if (!canceled)
                _isReloading = false;
        }

        private void CancelCurrentActions()
        {
            _tokenSource.Cancel();
            _tokenSource.Dispose();
            _tokenSource = new();
            _isReloading = false;
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/WeaponModule/Controllers/WeaponInventory.cs">
using System.Collections.Generic;
using Zenject;
using Cysharp.Threading.Tasks;
using InputModule;

namespace WeaponModule
{
    public class WeaponInventory : IInitializable, ITickable, ILateTickable
    {
        private readonly WeaponFactory _weaponFactory;
        private readonly IInputMap _input;
        private readonly List<WeaponSetupData> _loadout;
        private readonly List<IWeapon> _weapons = new();

        private IWeapon _currentWeapon;
        private int _currentIndex = -1;
        private bool _isSwitching = false;

        public WeaponInventory(WeaponFactory weaponFactory,
            IInputMap input,
            List<WeaponSetupData> loadout)
        {
            _weaponFactory = weaponFactory;
            _input = input;
            _loadout = loadout;
        }

        public void Initialize()
        {
            foreach (var setup in _loadout)
            {
                var weapon = _weaponFactory.Create(setup.Config, setup.View);
                weapon.Initialize();
                setup.View.gameObject.SetActive(false);
                _weapons.Add(weapon);
            }

            if (_weapons.Count > 0)
                EquipWeapon(0).Forget();
        }

        public void Tick()
        {
            if (_isSwitching)
                return;

            _currentWeapon?.Tick();
            HandleInput();
        }

        public void LateTick() => _currentWeapon?.LateTick();

        private void HandleInput()
        {
            int requestedIndex = _input.SelectWeaponIndex;

            if (requestedIndex != -1)
            {
                EquipWeapon(requestedIndex).Forget();
                return;
            }

            float scroll = _input.WeaponScroll;

            //FIXME: Magic numbers
            if (scroll > 0.1f)
                EquipNext();
            else if (scroll < -0.1f)
                EquipPrevious();
        }

        private void EquipNext()
        {
            //FIXME: Magic numbers
            if (_currentIndex == -1)
            {
                EquipWeapon(0).Forget();
                return;
            }

            //FIXME: Magic numbers
            int nextIndex = _currentIndex + 1;

            if (nextIndex >= _weapons.Count)
                nextIndex = 0;

            EquipWeapon(nextIndex).Forget();
        }

        private void EquipPrevious()
        {
            if (_currentIndex == -1)
            {
                EquipWeapon(_weapons.Count - 1).Forget();
                return;
            }

            //FIXME: Magic numbers
            int prevIndex = _currentIndex - 1;

            //FIXME: Magic numbers
            if (prevIndex < 0)
                prevIndex = _weapons.Count - 1;

            EquipWeapon(prevIndex).Forget();
        }

        private async UniTaskVoid EquipWeapon(int index)
        {
            if (index < 0 || index >= _weapons.Count)
                return;

            _isSwitching = true;

            if (_weapons[index] == _currentWeapon)
            {
                if (_currentWeapon != null)
                    await _currentWeapon.Unequip();

                _currentWeapon = null;
                //FIXME: Magic numbers
                _currentIndex = -1;

                _isSwitching = false;
                return;
            }

            if (_currentWeapon != null)
                await _currentWeapon.Unequip();

            _currentIndex = index;
            _currentWeapon = _weapons[index];
            _currentWeapon.Equip();
            _isSwitching = false;
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/WeaponModule/Core/IWeapon.cs">
using Cysharp.Threading.Tasks;

namespace WeaponModule
{
    public interface IWeapon
    {
        void Initialize();
        void Tick();
        void LateTick();
        void Equip();
        UniTask Unequip();
    }
}
</file>

<file path="Assets/_Main/Scripts/WeaponModule/Core/WeaponFactory.cs">
using Zenject;

namespace WeaponModule
{
    public class WeaponFactory : PlaceholderFactory<WeaponConfig, WeaponView, WeaponController>
    {
    }
}
</file>

<file path="Assets/_Main/Scripts/WeaponModule/Core/WeaponModel.cs">
using System;

namespace WeaponModule
{
    public class WeaponModel
    {
        private int _currentAmmo;
        private int _maxAmmo;

        public event Action<int, int> AmmoChanged;

        public int CurrentAmmo => _currentAmmo;
        public bool IsFull => _currentAmmo == _maxAmmo;
        public bool IsEmpty => _currentAmmo <= 0;

        public void Initialize(int maxAmmo)
        {
            _maxAmmo = maxAmmo;
            _currentAmmo = maxAmmo;
            AmmoChanged?.Invoke(_currentAmmo, _maxAmmo);
        }

        public bool TryConsumeAmmo()
        {
            if (_currentAmmo <= 0)
                return false;

            _currentAmmo--;
            AmmoChanged?.Invoke(_currentAmmo, _maxAmmo);
            return true;
        }

        public void Reload()
        {
            _currentAmmo = _maxAmmo;
            AmmoChanged?.Invoke(_currentAmmo, _maxAmmo);
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/WeaponModule/Data/CameraRecoilSettings.cs">
using UnityEngine;

namespace WeaponModule
{
    [System.Serializable]
    public class CameraRecoilSettings
    {
        public Vector3 RecoilAmount = new Vector3(-2f, 2f, 0.5f);
        public float Snappiness = 6f;
        public float ReturnSpeed = 2f;
    }
}
</file>

<file path="Assets/_Main/Scripts/WeaponModule/Data/RecoilSettings.cs">
using UnityEngine;

namespace WeaponModule
{
    [System.Serializable]
    public class RecoilSettings
    {
        public Vector3 RecoilRotation = new Vector3(-2f, 2f, 2f);
        public float KickBackZ = 0.2f;
        public float Snappiness = 6f;
        public float ReturnSpeed = 2f;
    }
}
</file>

<file path="Assets/_Main/Scripts/WeaponModule/Data/SwaySettings.cs">
using UnityEngine;

namespace WeaponModule
{
    [System.Serializable]
    public class SwaySettings
    {
        [Header("Position")]
        public float Step = 0.02f;
        public float MaxStep = 0.06f;
        public float Smooth = 8f;

        [Header("Rotation")]
        public float RotationStep = 4f;
        public float MaxRotation = 10f;
        public float SmoothRot = 10f;

        [Header("Tilt (Z)")]
        public float Tilt = 2f;
        public float MaxTilt = 5f;
    }
}
</file>

<file path="Assets/_Main/Scripts/WeaponModule/Data/WeaponConfig.cs">
using UnityEngine;

namespace WeaponModule
{
    [CreateAssetMenu(fileName = "WeaponConfig", menuName = "Configs/Weapon")]
    public class WeaponConfig : ScriptableObject
    {
        [Header("Stats")]
        [SerializeField] private float damage = 20f;
        [SerializeField] private float fireRate = 0.1f;
        [SerializeField] private int maxAmmo = 30;
        [SerializeField] private float bulletSpeed = 200f;

        [Header("Physics")] [Range(0f, 1f)]
        [SerializeField] private float inheritVelocity = 0.5f;

        [Header("Timings")]
        [SerializeField] private float reloadTime = 2.0f;
        [SerializeField] private float drawTime = 1.0f;

        [Header("Aiming")]
        [SerializeField]private Vector3 aimPosition;
        [SerializeField] private Vector3 aimRotation;
        [SerializeField] private float aimSpeed = 10f;
        [SerializeField] private bool aimIsToggle = false;
        [Range(0f, 1f)] [SerializeField] private float aimStability = 0.8f;

        [Header("--- Recoil & Sway ---")]
        public RecoilSettings HipRecoil;
        public RecoilSettings AimRecoil;
        public CameraRecoilSettings CamHipRecoil;
        public CameraRecoilSettings CamAimRecoil;
        public SwaySettings Sway;

        public float Damage => damage;
        public float FireRate => fireRate;
        public int MaxAmmo => maxAmmo;
        public float BulletSpeed => bulletSpeed;
        public float InheritVelocity => inheritVelocity;
        public float ReloadTime => reloadTime;
        public float DrawTime => drawTime;

        public Vector3 AimPosition => aimPosition;
        public Vector3 AimRotation => aimRotation;
        public float AimSpeed => aimSpeed;
        public bool AimIsToggle => aimIsToggle;
        public float AimStability => aimStability;
    }
}
</file>

<file path="Assets/_Main/Scripts/WeaponModule/Data/WeaponSetupData.cs">
namespace WeaponModule
{
    [System.Serializable]
    public class WeaponSetupData
    {
        public string Name;
        public WeaponConfig Config;
        public WeaponView View;
    }
}
</file>

<file path="Assets/_Main/Scripts/WeaponModule/Installers/WeaponInstaller.cs">
using UnityEngine;
using Zenject;
using System.Collections.Generic;

namespace WeaponModule.Installers
{
    public class WeaponInstaller : MonoInstaller
    {
        [SerializeField] private List<WeaponSetupData> loadout;

        public override void InstallBindings()
        {
            Container.BindInstance(loadout).AsSingle();

            Container.Bind<WeaponModel>().AsTransient();

            Container.BindFactory<WeaponConfig, WeaponView, WeaponController, WeaponFactory>();

            Container.BindInterfacesTo<WeaponInventory>().AsSingle();
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/WeaponModule/View/Procedural/RecoilProcessor.cs">
using UnityEngine;

namespace WeaponModule
{
    public class RecoilProcessor
    {
        private Vector3 _targetRotation;
        private Vector3 _currentRotation;
        private Vector3 _targetPosition;
        private Vector3 _currentPosition;

        private float _snappiness;
        private float _returnSpeed;

        public Vector3 CurrentRotation => _currentRotation;
        public Vector3 CurrentPosition => _currentPosition;

        public void Update(float deltaTime)
        {
            _targetRotation = Vector3.Lerp(_targetRotation, Vector3.zero, _returnSpeed * deltaTime);
            _currentRotation = Vector3.Slerp(_currentRotation, _targetRotation, _snappiness * deltaTime);

            _targetPosition = Vector3.Lerp(_targetPosition, Vector3.zero, _returnSpeed * deltaTime);
            _currentPosition = Vector3.Lerp(_currentPosition, _targetPosition, _snappiness * deltaTime);
        }

        public void Fire(RecoilSettings settings)
        {
            _snappiness = settings.Snappiness;
            _returnSpeed = settings.ReturnSpeed;

            _targetRotation += new Vector3(settings.RecoilRotation.x,
                Random.Range(-settings.RecoilRotation.y, settings.RecoilRotation.y),
                Random.Range(-settings.RecoilRotation.z, settings.RecoilRotation.z));

            _targetPosition += new Vector3(0, 0, -settings.KickBackZ);
        }

        public void FireCamera(CameraRecoilSettings settings)
        {
            _snappiness = settings.Snappiness;
            _returnSpeed = settings.ReturnSpeed;

            _targetRotation += new Vector3(settings.RecoilAmount.x,
                Random.Range(-settings.RecoilAmount.y, settings.RecoilAmount.y),
                Random.Range(-settings.RecoilAmount.z, settings.RecoilAmount.z));
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/WeaponModule/View/Procedural/SwayProcessor.cs">
using UnityEngine;

namespace WeaponModule
{
    public class SwayProcessor
    {
        private Vector3 _currentPos;
        private Quaternion _currentRot;

        public Vector3 OutputPosition => _currentPos;
        public Quaternion OutputRotation => _currentRot;

        public void Update(Vector2 inputDelta, SwaySettings settings, float deltaTime, bool isAiming)
        {
            //FIXME: Magic numbers
            float multiplier = isAiming ? 0.1f : 1f;

            float moveX = Mathf.Clamp(-inputDelta.x * settings.Step * multiplier, -settings.MaxStep, settings.MaxStep);
            float moveY = Mathf.Clamp(-inputDelta.y * settings.Step * multiplier, -settings.MaxStep, settings.MaxStep);

            Vector3 finalPos = new Vector3(moveX, moveY, 0);
            _currentPos = Vector3.Lerp(_currentPos, finalPos, deltaTime * settings.Smooth);

            float rotationX = Mathf.Clamp(-inputDelta.y * settings.RotationStep * multiplier, -settings.MaxRotation, settings.MaxRotation);
            float rotationY = Mathf.Clamp(inputDelta.x * settings.RotationStep * multiplier, -settings.MaxRotation, settings.MaxRotation);
            float rotationZ = Mathf.Clamp(-inputDelta.x * settings.Tilt * multiplier, -settings.MaxTilt, settings.MaxTilt);

            Quaternion finalRot = Quaternion.Euler(rotationX, rotationY, rotationZ);
            _currentRot = Quaternion.Slerp(_currentRot, finalRot, deltaTime * settings.SmoothRot);
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/WeaponModule/View/WeaponView.cs">
using UnityEngine;

namespace WeaponModule
{
    public class WeaponView : MonoBehaviour
    {
        [Header("References")] [SerializeField] private Animator handsAnimator;
        [SerializeField] private Animator gunAnimator;
        [SerializeField] private ParticleSystem muzzleFlash;
        [SerializeField] private Transform firePoint;
        [SerializeField] private Transform aimPivot;

        [Header("Recoil Targets")] [SerializeField] private Transform recoilPivot;
        [SerializeField] private GameObject bulletPrefab;

        private readonly RecoilProcessor _weaponRecoil = new();
        private readonly RecoilProcessor _cameraRecoil = new();
        private readonly SwayProcessor _sway = new();

        private Vector3 _hipPosition;
        private Quaternion _hipRotation;
        private WeaponConfig _config;
        private float _currentAimBlend = 0f;
        private CharacterController _playerCharacter;

        private Transform _cameraTransform;

        public void Initialize(WeaponConfig config)
        {
            _config = config;
            _hipPosition = aimPivot.localPosition;
            _hipRotation = aimPivot.localRotation;

            //TODO: Через SerializeField
            _playerCharacter = GetComponentInParent<CharacterController>();
            _cameraTransform = Camera.main!.transform.parent;
        }

        //FIXME: Unused method
        public void SetActive(bool isActive) => gameObject.SetActive(isActive);

        public void SetTriggerHold(bool isHeld)
        {
            //TODO: Вынести в отдельный класс параметры аниматора и делать через хэш
            handsAnimator?.SetBool("TriggerHold", isHeld);
        }

        public void PlayFireEffects(bool isAiming)
        {
            //TODO: Вынести в отдельный класс параметры аниматора и делать через хэш
            //FIXME: Magic numbers
            gunAnimator?.Play("Fire", 1, 0f);

            if (muzzleFlash)
                muzzleFlash.Play();

            if (isAiming)
            {
                _weaponRecoil.Fire(_config.AimRecoil);
                _cameraRecoil.FireCamera(_config.CamAimRecoil);
            }
            else
            {
                _weaponRecoil.Fire(_config.HipRecoil);
                _cameraRecoil.FireCamera(_config.CamHipRecoil);
            }
        }

        public void PlayReload()
        {
            //TODO: Вынести в отдельный класс параметры аниматора и делать через хэш
            handsAnimator?.SetTrigger("Reload");
            gunAnimator?.SetTrigger("Reload");
        }

        public void SetHolsterState(bool isHolstered)
        {
            //TODO: Вынести в отдельный класс параметры аниматора и делать через хэш
            string trigger = isHolstered ? "Holster" : "Draw";

            handsAnimator?.SetTrigger(trigger);
            gunAnimator?.SetTrigger(trigger);
        }

        public void SetMovementState(bool isMoving, Vector2 inputVector)
        {
            //TODO: Вынести в отдельный класс параметры аниматора и делать через хэш
            //FIXME: Magic numbers
            if (handsAnimator)
            {
                handsAnimator.SetBool("IsMoving", isMoving);

                handsAnimator.SetFloat("InputX", inputVector.x, 0.1f, Time.deltaTime);
                handsAnimator.SetFloat("InputY", inputVector.y, 0.1f, Time.deltaTime);
            }

            if (!gunAnimator)
                return;

            gunAnimator.SetBool("IsMoving", isMoving);
            gunAnimator.SetFloat("InputX", inputVector.x, 0.1f, Time.deltaTime);
            gunAnimator.SetFloat("InputY", inputVector.y, 0.1f, Time.deltaTime);
        }


        public void UpdateAiming(bool isAiming, float stabilityTarget)
        {
            Vector3 targetPos = isAiming ? _config.AimPosition : _hipPosition;
            Quaternion targetRot = isAiming ? Quaternion.Euler(_config.AimRotation) : _hipRotation;

            float speed = _config.AimSpeed * Time.deltaTime;
            aimPivot.localPosition = Vector3.Lerp(aimPivot.localPosition, targetPos, speed);
            aimPivot.localRotation = Quaternion.Slerp(aimPivot.localRotation, targetRot, speed);
            float targetBlend = isAiming ? stabilityTarget : 0f;

            _currentAimBlend = Mathf.Lerp(_currentAimBlend, targetBlend, Time.deltaTime * 10f);
            if (handsAnimator) handsAnimator.SetFloat("AimBlend", _currentAimBlend);
            if (gunAnimator) gunAnimator.SetFloat("AimBlend", _currentAimBlend);
        }

        public void UpdateProcedural(float deltaTime, Vector2 lookInput, bool isAiming)
        {
            _weaponRecoil.Update(deltaTime);
            _cameraRecoil.Update(deltaTime);
            _sway.Update(lookInput, _config.Sway, deltaTime, isAiming);
            recoilPivot.localPosition = _weaponRecoil.CurrentPosition + _sway.OutputPosition;
            recoilPivot.localRotation = Quaternion.Euler(_weaponRecoil.CurrentRotation) * _sway.OutputRotation;

            if (_cameraTransform != null)
            {
                _cameraTransform.localRotation = Quaternion.Euler(_cameraRecoil.CurrentRotation);
            }
        }

        public void SpawnBullet(float damage, float speed)
        {
            if (bulletPrefab == null || firePoint == null) return;

            GameObject bulletObj = Instantiate(bulletPrefab, firePoint.position, firePoint.rotation);

            if (bulletObj.TryGetComponent(out Bullet bulletScript))
            {
                Vector3 playerVelocity = _playerCharacter != null ? _playerCharacter.velocity : Vector3.zero;
                bulletScript.Setup(damage, speed, _config.InheritVelocity, playerVelocity);
            }
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/WeaponModule/WeaponModule.asmdef">
{
    "name": "WeaponModule",
    "rootNamespace": "",
    "references": [
        "GUID:0d8beb7f090555447a6cf5ce9e54dbb4",
        "GUID:f51ebe6a0ceec4240a699833d6309b23",
        "GUID:5a8d3845af48dc54db440d2b68ad43e7",
        "GUID:bae74e8de93bc434190a94426fd359de",
        "GUID:61af276ff4b779247aca1e9043c620bc",
        "GUID:ad92713fd5884a16a1e7ccde1a579228"
    ],
    "includePlatforms": [],
    "excludePlatforms": [],
    "allowUnsafeCode": false,
    "overrideReferences": false,
    "precompiledReferences": [],
    "autoReferenced": true,
    "defineConstraints": [],
    "versionDefines": [],
    "noEngineReferences": false
}
</file>

<file path="Assets/_Main/Scripts/ComponentsModule/ComponentsModule.asmdef">
{
    "name": "ComponentsModule",
    "rootNamespace": "",
    "references": [],
    "includePlatforms": [],
    "excludePlatforms": [],
    "allowUnsafeCode": false,
    "overrideReferences": false,
    "precompiledReferences": [],
    "autoReferenced": true,
    "defineConstraints": [],
    "versionDefines": [],
    "noEngineReferences": false
}
</file>

<file path="Assets/_Main/Scripts/InputModule/Core/IInputService.cs">
namespace InputModule
{
    public interface IInputService
    {
        void Enable();
        void Disable();
        void SetUIMode(bool isActive);
    }
}
</file>

<file path="Assets/_Main/Scripts/InventoryModule/Runtime/Core/InventoryItem.cs">
using UnityEngine;

public class InventoryItem
{
    public string Id { get; }
    public Vector2Int Position { get; internal set; }
    private readonly int _baseWidth;
    private readonly int _baseHeight;
    public int MaxStack { get; }
    public int Amount { get; set; }
    public bool IsRotated { get; internal set; }

    public Vector2Int Size => IsRotated
        ? new Vector2Int(_baseHeight, _baseWidth)
        : new Vector2Int(_baseWidth, _baseHeight);

    public Vector2Int BaseSize => new Vector2Int(_baseWidth, _baseHeight);

    public Vector2Int GetSize(bool rotated)
    {
        return rotated
            ? new Vector2Int(_baseHeight, _baseWidth)
            : new Vector2Int(_baseWidth, _baseHeight);
    }

    public InventoryItem(string id, Vector2Int position, Vector2Int size, int maxStack, int amount)
    {
        Id = id;
        Position = position;
        _baseWidth = size.x;
        _baseHeight = size.y;
        MaxStack = maxStack;
        Amount = amount;
        IsRotated = false;
    }

    public bool TryAddAmount(int value, out int remainder)
    {
        int space = MaxStack - Amount;
        int toAdd = Mathf.Min(space, value);
        Amount += toAdd;
        remainder = value - toAdd;
        return remainder < value;
    }
}
</file>

<file path="Assets/_Main/Scripts/InventoryModule/Runtime/Core/InventoryModel.cs">
using System;
using System.Collections.Generic;

namespace InventoryModule
{
    public sealed class InventoryModel
    {
        private readonly List<InventoryItem> _items = new();

        public int Width { get; }
        public int Height { get; }
        public IReadOnlyList<InventoryItem> Items => _items;

        public InventoryModel(int width, int height)
        {
            if (width <= 0 || height <= 0)
                throw new ArgumentException("Inventory dimensions must be positive");

            Width = width;
            Height = height;
        }

        public void AddItem(InventoryItem item)
        {
            if (item == null)
                throw new ArgumentNullException(nameof(item));

            if (_items.Contains(item))
                throw new InvalidOperationException("Item already exists in inventory");

            _items.Add(item);
        }

        public void RemoveItem(InventoryItem item)
        {
            if (item == null)
                throw new ArgumentNullException(nameof(item));

            if (!_items.Contains(item))
                throw new InvalidOperationException("Item not found in inventory");

            _items.Remove(item);
        }

        public void Clear()
        {
            _items.Clear();
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/InventoryModule/Runtime/Presenter/InventoryPresenter.cs">
using System;
using System.Threading;
using InputModule;
using UnityEngine;
using Zenject;

namespace InventoryModule
{
    public sealed class InventoryPresenter : IInitializable, ITickable, IDisposable
    {
        private readonly IInventoryService _inventoryService;
        private readonly IItemProvider _itemProvider;
        private readonly InventoryView _view;
        private readonly IInputMap _inputMap;
        private readonly IInputService _inputService;
        private readonly CancellationTokenSource _cancellationTokenSource;

        public InventoryPresenter(
            IInventoryService inventoryService,
            IItemProvider itemProvider,
            InventoryView view,
            IInputMap inputMap,
            IInputService inputService)
        {
            _inventoryService = inventoryService ?? throw new ArgumentNullException(nameof(inventoryService));
            _itemProvider = itemProvider ?? throw new ArgumentNullException(nameof(itemProvider));
            _view = view ?? throw new ArgumentNullException(nameof(view));
            _inputMap = inputMap ?? throw new ArgumentNullException(nameof(inputMap));
            _inputService = inputService ?? throw new ArgumentNullException(nameof(inputService));

            _cancellationTokenSource = new CancellationTokenSource();
        }

        public void Initialize()
        {
            _inventoryService.InventoryUpdated += OnInventoryUpdated;
            _view.ItemDropped += OnItemDropped;
            _view.ItemRemoved += OnItemRemoved;

            OnInventoryUpdated();
        }

        public void Dispose()
        {
            _cancellationTokenSource.Cancel();
            _cancellationTokenSource.Dispose();

            _inventoryService.InventoryUpdated -= OnInventoryUpdated;
            _view.ItemDropped -= OnItemDropped;
            _view.ItemRemoved -= OnItemRemoved;
        }

        public void Tick()
        {
            if (_inputMap.IsInventoryPressed)
            {
                bool newState = !_view.IsVisible;
                _view.Toggle(newState);
                _inputService.SetUIMode(newState);
            }
        }

        private void OnInventoryUpdated()
        {
            var items = _inventoryService.GetAllItems();
            _view.Render(items, _itemProvider);
        }

        private async void OnItemDropped(InventoryItemView itemView, Vector2Int newPosition)
        {
            var item = itemView.Item;
            if (item == null)
                return;

            InventoryOperationResult result;

            if (itemView.IsSplitting)
            {
                result = await _inventoryService.SplitItemAsync(
                    item,
                    newPosition,
                    itemView.LocalIsRotated,
                    _cancellationTokenSource.Token);
            }
            else
            {
                result = await _inventoryService.MoveItemAsync(
                    item,
                    newPosition,
                    itemView.LocalIsRotated,
                    _cancellationTokenSource.Token);
            }

            if (!result.Success)
            {
                Debug.LogWarning($"Inventory operation failed: {result.ErrorMessage}");
                OnInventoryUpdated();
            }
        }

        private async void OnItemRemoved(InventoryItemView itemView)
        {
            var result = await _inventoryService.RemoveItemAsync(
                itemView.Item,
                _cancellationTokenSource.Token);

            if (!result.Success)
            {
                Debug.LogWarning($"Failed to remove item: {result.ErrorMessage}");
            }
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/InventoryModule/Runtime/View/InventoryItemView.cs">
using UnityEngine;
using UnityEngine.EventSystems;
using UnityEngine.UI;
using TMPro;

namespace InventoryModule
{
    public class InventoryItemView : MonoBehaviour, IBeginDragHandler, IDragHandler, IEndDragHandler
    {
        [Header("References")]
        [SerializeField] private Image iconImage;
        [SerializeField] private TMP_Text amountText;
        [SerializeField] private RectTransform rectTransform;
        [SerializeField] private CanvasGroup canvasGroup;

        [Header("Settings")]
        [Range(0f, 1f)]
        [SerializeField] private float dragAlpha = 0.7f;
        [Range(0f, 1f)]
        [SerializeField] private float splitGhostAlpha = 0.5f;

        public event System.Action<InventoryItemView> DragStarted;
        public event System.Action<InventoryItemView> DragEnded;
        public event System.Action<InventoryItemView> DragUpdated;

        private InventoryItem _item;
        private float _tileSize;
        private float _spacing;
        private InputModule.IInputMap _input;

        private Vector2 _centeringOffset;
        private bool _localIsRotated;
        private bool _isSplitting;
        private int _originalAmount;
        private bool _isDragging;
        private bool _rotatePressedLastFrame;

        private GameObject _splitDummy;

        public InventoryItem Item => _item;
        public bool IsSplitting => _isSplitting;
        public bool LocalIsRotated => _localIsRotated;

        public void Setup(InventoryItem item, ItemDefinition config, float tileSize, float spacing, InputModule.IInputMap input)
        {
            _item = item;
            _tileSize = tileSize;
            _spacing = spacing;
            _input = input;

            _localIsRotated = item.IsRotated;
            _originalAmount = item.Amount;
            _isDragging = false;
            _rotatePressedLastFrame = false;

            if (iconImage != null)
                iconImage.sprite = config.Icon;

            UpdateVisuals();
        }

        private void Update()
        {
            if (!_isDragging) return;

            bool rotatePressed = _input.IsRotatePressed;

            if (rotatePressed && !_rotatePressedLastFrame)
            {
                _localIsRotated = !_localIsRotated;

                UpdateVisuals();
                RecalculateOffset();
                SnapToCursor();

                DragUpdated?.Invoke(this);
            }

            _rotatePressedLastFrame = rotatePressed;
        }

        private void UpdateVisuals()
        {
            if (_item == null) return;

            int baseW = _item.IsRotated ? _item.Size.y : _item.Size.x;
            int baseH = _item.IsRotated ? _item.Size.x : _item.Size.y;
            int currentW = _localIsRotated ? baseH : baseW;
            int currentH = _localIsRotated ? baseW : baseH;

            float widthPx = (currentW * _tileSize) + ((currentW - 1) * _spacing);
            float heightPx = (currentH * _tileSize) + ((currentH - 1) * _spacing);

            rectTransform.sizeDelta = new Vector2(widthPx, heightPx);

            if (!_isDragging)
            {
                float posX = (_item.Position.x * _tileSize) + (_item.Position.x * _spacing);
                float posY = -((_item.Position.y * _tileSize) + (_item.Position.y * _spacing));
                rectTransform.anchoredPosition = new Vector2(posX, posY);
            }

            if (amountText != null)
            {
                int showAmount = _isSplitting ? (_originalAmount / 2) : _item.Amount;
                if (showAmount > 1)
                {
                    amountText.gameObject.SetActive(true);
                    amountText.text = showAmount.ToString();
                }
                else
                {
                    amountText.gameObject.SetActive(false);
                }
            }
        }

        public void OnBeginDrag(PointerEventData eventData)
        {
            _isDragging = true;
            _isSplitting = _input.IsSplitPressed && _item.Amount > 1;

            if (_isSplitting)
            {
                CreateSplitDummy();
            }

            canvasGroup.blocksRaycasts = false;
            canvasGroup.alpha = dragAlpha;

            UpdateVisuals();
            RecalculateOffset();
            SnapToCursor();

            DragStarted?.Invoke(this);
        }

        public void OnDrag(PointerEventData eventData)
        {
            SnapToCursor();
            DragUpdated?.Invoke(this);
        }

        public void OnEndDrag(PointerEventData eventData)
        {
            _isDragging = false;
            canvasGroup.blocksRaycasts = true;
            canvasGroup.alpha = 1f;

            if (_splitDummy != null)
                Destroy(_splitDummy);

            DragEnded?.Invoke(this);
        }

        private void CreateSplitDummy()
        {
            _splitDummy = Instantiate(gameObject, transform.parent);

            Destroy(_splitDummy.GetComponent<InventoryItemView>());
            var cg = _splitDummy.GetComponent<CanvasGroup>();
            if (cg == null) cg = _splitDummy.AddComponent<CanvasGroup>();
            cg.alpha = splitGhostAlpha;
            cg.blocksRaycasts = false;

            var dummyRect = _splitDummy.GetComponent<RectTransform>();
            dummyRect.anchoredPosition = rectTransform.anchoredPosition;
            dummyRect.sizeDelta = rectTransform.sizeDelta;

            var dummyText = _splitDummy.GetComponentInChildren<TMP_Text>();
            int stayingAmount = _originalAmount - (_originalAmount / 2);
            if (stayingAmount > 1)
            {
                dummyText.gameObject.SetActive(true);
                dummyText.text = stayingAmount.ToString();
            }
            else
            {
                dummyText.gameObject.SetActive(false);
            }
        }

        private void RecalculateOffset()
        {
            var rect = rectTransform.rect;
            _centeringOffset = new Vector2(-rect.width * 0.5f, rect.height * 0.5f);
        }

        private void SnapToCursor()
        {
            var parentRT = transform.parent as RectTransform;
            if (parentRT == null) return;

            if (RectTransformUtility.ScreenPointToLocalPointInRectangle(
                parentRT,
                Input.mousePosition,
                null,
                out var localMousePos))
            {
                rectTransform.anchoredPosition = localMousePos + _centeringOffset;
            }
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/InventoryModule/Runtime/View/InventoryView.cs">
using InputModule;
using System;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

namespace InventoryModule
{
    public sealed class InventoryView : MonoBehaviour
    {
        [SerializeField] private GameObject windowRoot;
        [SerializeField] private RectTransform itemsContainer;
        [SerializeField] private RectTransform gridBackground;
        [SerializeField] private InventoryItemView itemPrefab;
        [SerializeField] private GameObject slotPrefab;
        [SerializeField] private float tileSize = 64f;
        [SerializeField] private float spacing = 1f;

        public event Action<InventoryItemView, Vector2Int> ItemDropped;
        public event Action<InventoryItemView> ItemRemoved;

        private readonly List<InventoryItemView> _spawnedItems = new();
        private IItemProvider _itemProvider;
        private IInputMap _input;

        public bool IsVisible => windowRoot.activeSelf;

        public void Initialize(IItemProvider itemProvider, IInputMap input)
        {
            _itemProvider = itemProvider;
            _input = input;
            Hide();
        }

        public void GenerateGrid(int width, int height)
        {
            foreach (Transform child in gridBackground)
                Destroy(child.gameObject);

            var layout = gridBackground.GetComponent<GridLayoutGroup>();
            if (layout != null)
            {
                layout.cellSize = new Vector2(tileSize, tileSize);
                layout.spacing = new Vector2(spacing, spacing);
                layout.constraint = GridLayoutGroup.Constraint.FixedColumnCount;
                layout.constraintCount = width;
            }

            float totalWidth = (width * tileSize) + ((width - 1) * spacing);
            float totalHeight = (height * tileSize) + ((height - 1) * spacing);

            Vector2 size = new Vector2(totalWidth, totalHeight);
            gridBackground.sizeDelta = size;
            itemsContainer.sizeDelta = size;

            int totalSlots = width * height;
            for (int i = 0; i < totalSlots; i++)
            {
                Instantiate(slotPrefab, gridBackground);
            }
        }

        public void Toggle(bool state)
        {
            windowRoot.SetActive(state);
            Cursor.visible = state;
            Cursor.lockState = state ? CursorLockMode.None : CursorLockMode.Locked;
        }

        public void Hide() => Toggle(false);

        public void Render(IReadOnlyList<InventoryItem> items, IItemProvider itemProvider)
        {
            ClearItems();

            foreach (var item in items)
            {
                var itemDef = itemProvider.GetItemDefinition(item.Id);
                if (itemDef == null) continue;

                var itemView = Instantiate(itemPrefab, itemsContainer);
                itemView.Setup(item, itemDef, tileSize, spacing, _input);
                itemView.DragStarted += OnItemDragStarted;
                itemView.DragEnded += OnItemDragEnded;
                itemView.DragUpdated += OnItemDragUpdated;
                _spawnedItems.Add(itemView);
            }
        }

        private void OnItemDragStarted(InventoryItemView itemView)
        {
            itemView.transform.SetParent(windowRoot.transform);
        }

        private void OnItemDragUpdated(InventoryItemView itemView)
        {
            // Optional: visual feedback during drag
        }

        private void OnItemDragEnded(InventoryItemView itemView)
        {
            Vector2Int gridPos = GetGridPosition(itemView);
            ItemDropped?.Invoke(itemView, gridPos);
        }

        private Vector2Int GetGridPosition(InventoryItemView itemView)
        {
            RectTransformUtility.ScreenPointToLocalPointInRectangle(
                itemsContainer,
                Input.mousePosition,
                null,
                out var localMousePos
            );
            var rect = itemView.GetComponent<RectTransform>().rect;
            float itemTopLeftX = localMousePos.x - (rect.width * 0.5f);
            float itemTopLeftY = localMousePos.y + (rect.height * 0.5f);

            float cellSize = tileSize + spacing;

            int x = Mathf.RoundToInt(itemTopLeftX / cellSize);
            int y = Mathf.RoundToInt(-itemTopLeftY / cellSize);

            return new Vector2Int(x, y);
        }

        private void ClearItems()
        {
            foreach (var view in _spawnedItems)
            {
                if (view != null) Destroy(view.gameObject);
            }
            _spawnedItems.Clear();
        }

        public Vector2Int GetMouseGridPosition()
        {
            RectTransformUtility.ScreenPointToLocalPointInRectangle(
                itemsContainer,
                Input.mousePosition,
                null,
                out Vector2 localPoint
            );

            float step = tileSize + spacing;
            int x = Mathf.FloorToInt(localPoint.x / step);
            int y = Mathf.FloorToInt(-localPoint.y / step);

            return new Vector2Int(x, y);
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/PlayerModule/PlayerModule.asmdef">
{
    "name": "PlayerModule",
    "rootNamespace": "",
    "references": [
        "GUID:ad92713fd5884a16a1e7ccde1a579228",
        "GUID:5a8d3845af48dc54db440d2b68ad43e7",
        "GUID:0d8beb7f090555447a6cf5ce9e54dbb4",
        "GUID:a052d9b9b243434c92f8e11262715c1f",
        "GUID:f952cb0599c86ac4794b61418ea81496",
        "GUID:f51ebe6a0ceec4240a699833d6309b23"
    ],
    "includePlatforms": [],
    "excludePlatforms": [],
    "allowUnsafeCode": false,
    "overrideReferences": false,
    "precompiledReferences": [],
    "autoReferenced": true,
    "defineConstraints": [],
    "versionDefines": [],
    "noEngineReferences": false
}
</file>

<file path="Assets/_Main/Scripts/PlayerModule/Runtime/Installers/PlayerInstaller.cs">
using ComponentsModule;
using UIModule;
using UnityEngine;
using Zenject;
using InventoryModule;

namespace PlayerModule
{
    public class PlayerInstaller : MonoInstaller
    {
        [Header("References")]
        [SerializeField] private CharacterController characterController;
        [SerializeField] private Transform cameraRoot;
        [SerializeField] private Transform player;
        
        [Header("Health")]
        [SerializeField] private HealthView healthView;
        
        [Header("Settings")]
        [SerializeField] private PlayerConfig config;

        private void OnValidate()
        {
            player ??= transform;
            characterController ??= GetComponent<CharacterController>();
        }

        public override void InstallBindings()
        {
            Container.Bind<PlayerConfig>()
                .FromInstance(config)
                .AsSingle();

            Container.Bind<CharacterController>()
                .FromInstance(characterController)
                .AsSingle();

            Container.Bind<Transform>()
                .FromInstance(player)
                .AsSingle();

            Container.Bind<IMoveComponent>()
                .To<MoveComponent>()
                .AsSingle()
                .WithArguments(config.WalkSpeed, config.JumpHeight, config.Gravity);

            Container.Bind<IRotationComponent>()
                .To<RotationComponent>()
                .AsSingle()
                .WithArguments(config.MouseSensitivity, config.LookXLimit);

            Container.Bind<ICrouchComponent>()
                .To<CrouchComponent>()
                .AsSingle()
                .WithArguments(cameraRoot, config.CrouchData);

            Container.Bind<ILeanComponent>()
                .To<LeanComponent>()
                .AsSingle()
                .WithArguments(cameraRoot, config.LeanAngle, config.LeanOffset, config.LeanSpeed);

            Container.Bind(typeof(IHealthComponent), typeof(IDamageable))
                .To<HealthComponent>()
                .AsSingle()
                .WithArguments(config.MaxHealth);

            Container.Bind<HealthView>()
                .FromInstance(healthView)
                .AsSingle();
            
            Container.BindInterfacesTo<HealthPresenter>()
                .AsSingle()
                .NonLazy();

            Container.BindInterfacesTo<PlayerMovementController>()
                .AsSingle()
                .NonLazy();

            Container.BindInterfacesAndSelfTo<PlayerInteractionController>()
                .AsSingle()
                .NonLazy();

            Container.BindInterfacesTo<InventoryDeathHandler>()
                .AsSingle()
                .NonLazy();

            Container.Bind<IInventoryService>()
                .FromResolve()
                .AsSingle();

            Container.Bind<IItemProvider>()
                .FromResolve()
                .AsSingle();
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/InputModule/Core/GameInputService.cs">
using UnityEngine;
using InputModule.Configs;

namespace InputModule
{
    public class GameInputService : IInputMap, IInputService
    {
        private readonly InputSystem_Actions _actions;

        public GameInputService()
        {
            _actions = new();
            _actions.Player.Enable();
            _actions.Global.Enable();
        }
        public void SetUIMode(bool isActive)
        {
            if (isActive)
            {
                _actions.Player.Disable();
            }
            else
            {
                _actions.Player.Enable();
            }
        }
        public Vector2 MoveInput => _actions.Player.Move.ReadValue<Vector2>();
        public Vector2 LookInput => _actions.Player.Look.ReadValue<Vector2>();
        public bool IsJumpPressed => _actions.Player.Jump.WasPressedThisFrame();
        public bool IsCrouchPressed => _actions.Player.Crouch.IsPressed();
        public float LeanInput => _actions.Player.Lean.ReadValue<float>();
        public bool IsInteractPressed => _actions.Player.Interact.WasPressedThisFrame();

        public bool IsFirePressed => _actions.Player.Attack.IsPressed();
        public bool IsAimPressed => _actions.Player.Aim.IsPressed();
        public bool IsAimHeld => _actions.Player.Aim.IsPressed();
        public bool IsAimTriggered => _actions.Player.Aim.WasPressedThisFrame();
        public bool IsReloadPressed => _actions.Player.Reload.WasPressedThisFrame();
        public bool IsHolsterPressed => _actions.Player.Holster.WasPressedThisFrame();

        public bool IsInventoryPressed => _actions.Global.ToggleInventory.WasPressedThisFrame();
        public bool IsRotatePressed => _actions.Global.RotateItem.WasPressedThisFrame();
        public bool IsSplitPressed => _actions.Global.SplitStack.IsPressed();

        public int SelectWeaponIndex
        {
            get
            {
                if (_actions.Player.Weapon1.WasPressedThisFrame()) return 0;
                if (_actions.Player.Weapon2.WasPressedThisFrame()) return 1;
                if (_actions.Player.Weapon3.WasPressedThisFrame()) return 2;

                return -1;
            }
        }

        public float WeaponScroll
        {
            get
            {
                float scrollValue = _actions.Player.WeaponScroll.ReadValue<float>();

                if (scrollValue > 0) return 1f;

                if (scrollValue < 0) return -1f;

                return 0f;
            }
        }

        public void Enable() => _actions.Enable();
        public void Disable() => _actions.Disable();

        public void Dispose()
        {
            _actions.Disable();
            _actions.Dispose();
        }
    }
}
</file>

<file path="Assets/_Main/Scripts/InputModule/Core/IInputMap.cs">
using UnityEngine;

namespace InputModule
{
    public interface IInputMap
    {
        Vector2 MoveInput { get; }
        Vector2 LookInput { get; }
        bool IsJumpPressed { get; }
        bool IsCrouchPressed { get; }
        float LeanInput { get; }
        bool IsInteractPressed { get; }

        float WeaponScroll { get; }
        int SelectWeaponIndex { get; }
        bool IsFirePressed { get; }
        bool IsAimPressed { get; }
        bool IsReloadPressed { get; }
        bool IsHolsterPressed { get; }
        bool IsAimHeld { get; }
        bool IsAimTriggered { get; }

        bool IsInventoryPressed { get; }
        bool IsRotatePressed { get; }
        bool IsSplitPressed { get; }
    }
}
</file>

</files>
